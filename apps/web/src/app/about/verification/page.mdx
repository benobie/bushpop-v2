export const metadata = {
  title: "Seller Verification — Bushpop",
  description:
    "How seller verification works on Bushpop and what it means for buyers.",
};

{/* OPEN QUESTION FOR BEN: audit verdict was DROP or REBUILD — the verification flow described on the old WordPress page relates to a seller-vetting process that may no longer run on the new stack. Confirm whether to keep or 410 this URL. If keeping, replace this placeholder with current process. */}

# Seller Verification

{/* TODO: Ben writes copy — audit verdict: AMBIGUOUS (DROP or REBUILD). Decision needed before publishing. */}

## What verification means

{/* TODO: Ben writes copy — What the old verification process was. Does it still apply? */}

## How sellers get verified

{/* TODO: Ben writes copy — Steps, requirements, timeline (if the process still exists). */}

## What buyers should look for

{/* TODO: Ben writes copy — Verified badge, ratings, listing quality signals. */}
