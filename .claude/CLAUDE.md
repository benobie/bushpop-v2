# bushpop-v2 — Bushpop Launch-1 Content Site

## What this repo is

`bushpop-v2` is the **Launch-1 static content site** for `bushpop.com.au` — a Next.js 16 + MDX app that replaces the WordPress site with a fast, SEO-preserving static export.

**This is NOT the marketplace.** The marketplace machinery (API, DB, Stripe, auth, workers) comes at Launch 2.

## Architecture

| Decision | Choice | Why |
|----------|--------|-----|
| Stack | Next.js 16, App Router, React 19, Tailwind v4, MDX | Same stack as Launch-2 marketplace — content pages graduate as routes, not a rewrite |
| Hosting | Cloudflare Pages, static export | Free, global CDN, same zone as bushpop.com.au |
| Output | `output: 'export'` (fully static, no Node runtime) | Best CWV for SEO-critical pages; Cloudflare Pages serves `out/` directly |
| Redirects | `apps/web/public/_redirects` ONLY | `next.config` `redirects()` are silently ignored under static export |
| Trailing slash | `trailingSlash: true` | Parity with WordPress URL contract; `/guides/size-charts/` → `out/guides/size-charts/index.html` |
| Content | MDX-in-repo, zero CMS | Git-versioned, zero attack surface, Ben authors/Claude commits |

## Locked architecture decisions

Full rationale: `~/projects/Bushpop/docs/architecture-decisions.md` (all six confirmed by Ben 16/06/2026).

## Monorepo structure

```
apps/web/       - Next.js 16 static content app (Launch 1 — CF Pages, static export)
apps/market/    - Marketplace SSR app (Launch 2 — piklo-v2 fork, flat routes, dev :3002)
packages/       - Engine: api (Fastify + workers), db (Drizzle), config, types, ui, api-client
infra/          - Engine compose: docker-compose.dev.yml (pg 5435 / redis 6380 / meili 7701),
                  docker-compose.engine.prod.yml (Coolify on homelab; web :3210, api :3334)
docs/engine/    - Curated upstream engine docs + FORK.md (provenance, pick recipe)
```

The engine was forked from `benobie/piklo-v2` @ `2419a38` (02/07/2026), renamed `@piklo/*` → `@bushpop/*`, single-tenant (only the `bushpop` channel exists; `CHANNEL_SLUG=bushpop`). **Read `docs/engine/FORK.md` before touching engine code** — notably the Stripe metadata keys (`piklo_payment_op_id` etc.) are deliberately NOT renamed (money-safety byte-parity with upstream) and must stay that way.

**Sell-flow backend SHIPPED 03/07 (PR #27, Phase 1 of `~/.claude/plans/build-brief-merry-garden.md`):** drafts façade (`routes/v1/seller/drafts/` — draft = inventory_items row, per-step PATCH, optimistic `version`), AI listing drafts (`lib/ai/` + `workers/ai-draft.ts` — Gemini 2.5 Flash-Lite primary, Haiku 4.5 escalation, worker gated on `GEMINI_API_KEY || ANTHROPIC_API_KEY`, writes ONLY `ai*` suggestion columns), image-variants worker (thumb-320/card-800/pdp-1600, always-on), shared strength v3 (`@bushpop/config` `computeListingStrength` — ONE rubric for drafts API/score worker/wizard, never fork it), publish gate (422 + `missing[]`), commission from `@bushpop/config` `COMMISSION_SCHEDULE` (1.75% + 30c — `channels.platform_fee_bps` is no longer consulted), migration `0023`. Gotcha for route authors: `@fastify/rate-limit` MUTATES the shared `preHandler` array — always build per-route arrays (see `drafts/routes.ts`).

**Sell wizard BUILT 04/07 (PR #48, Phase 2 of `~/.claude/plans/build-brief-merry-garden.md`):** the full `/sell` flow in `apps/market` — photos (compression/EXIF-strip/reorder/quality chips) → details (category/brand/size/colour/AI-assisted) → condition & measurements → price (live payout panel) → shipping → review/publish — backed by a Zustand draft-sync store (`lib/sell/store.ts`, single-flight debounced PATCH, optimistic updates, 409 dirty-wins conflict merge) against the drafts façade above. Publish gate mirrors the backend's `publishGateMissing()` client-side (Opus-verified byte-identical). Analytics wired through the existing PostHog `BushpopEvent` registry (`wizard.*` events), not GA4. Two dedicated Opus reviews (store engine, publish flow) plus a final whole-branch review, all APPROVE. 73 unit/RTL tests + 7 Playwright E2E tests against the real live app+API+Postgres stack (not mocks) — see `apps/market/e2e/sell-wizard.spec.ts`. Gotchas surfaced by running the E2E suite live rather than trusting mocks: the API dev server caches the `channels` table in memory at boot (`packages/api/src/plugins/channel.ts`) — reseeding after the server starts silently serves a fallback `channel_id: "unknown"` until restart; `publishDraft()` requires a `sellerProfiles` + `addresses` row (`assertListingActivationReady`), not just a `user_roles` grant. Next: multi-vendor / marketplace-wide features per the L2 direction (finish engine → single-seller → multi-vendor).

**Transactional email matrix CLOSED 05/07 (PR #62 merged; PR #65 open, HELD for Ben — B5, batch 37):** `docs/email-matrix.md` is the living gate doc. Two real gaps were found and fixed, both worse than flagged: the automated Starshipit/mock shipping-label path (`workers/shipping-label.ts`) never transitioned the order to `shipped` nor emailed the buyer (only the seller's manual mark-shipped path did); better-auth had **no** `sendVerificationEmail`/`sendResetPassword` callback configured at all, so password-reset 400'd outright and verification silently no-op'd. Both fixed + tested; account emails smoke-verified live against `api.bushpop.xyz`. Added `getFailedEmailJobs()` so the email queue's dead-letter state is queryable (previously invisible). The refund confirmation email (PR #65) — the one template of the original 8 that never existed — is wired into all 6 places an order can land refunded/cancelled, including both out-of-order webhook-reconciliation racing legs; cross-model reviewed, held for Ben's merge (money-adjacent).

**U0 design-system foundation MERGED 05/07 (PR #64, A3 §3-U0 ≡ styling-rollout PR-4):** ported `design/home/bushpop.css` (Bushpop/design repo) into the engine as the new BRG "Lit Glass" token layer — additive, not a replacement: `packages/config/tailwind/tokens.css` keeps the existing Coral `brand-*`/`trust-*`/`interactive` scale byte-identical (50+ live call sites incl. the sell wizard depend on it) and adds a parallel `--color-bp-*`/`--radius-bp-*`/`--font-bp-*` namespace with the locked BRG values (`--green-ink #004225`, `--green-bright #0b5c35`, CTA gradient, obsidian, deal-red). New `@bushpop/ui` primitives: `Button` restyled onto the F2 glass-pill primary + 12px glass-rect secondary system (existing variant names preserved — zero call-site breakage), `Chip`/`Tgl`/`Tlink`, `FoilBadge` (5 semantic variants), `Pcard` (product card, treatment D), `Rail` (flex-only scroll-snap — never grid), `SiteNav` (own port of `design/HANDOFF-nav.md` — 4 nav states, borderless-until-scroll, mobile drawer, session/data-driven props not hardcoded fixtures), `SiteFooter`, mobile bottom bar. Shared motion utilities (`useCursorLight`, `spawnBurst`, `useNavScrolled`) in `packages/ui/src/lib/`; all recipes in one stylesheet `packages/ui/src/styles/lit-glass.css` (imported via `apps/market/src/app/globals.css`). **Gotcha:** `@phosphor-icons/react` broke `next build` under this app's `cacheComponents`/Turbopack combo (every route failed page-data-collection with a `createContext` TypeError) — icons are hand-authored local SVG components instead (`packages/ui/src/icons/`), using the prototype's own exact path data; see `docs/HANDOFF-ZERO-CONTEXT.md` §11 (05/07 entry) for the full trace before reaching for that package again (B11 apps/web re-skin is the next likely icon-touching session). **Not done, deliberately deferred to U1:** `SiteNav`/`SiteFooter`/mobile bar aren't wired into the live `apps/market/src/app/layout.tsx` yet (still the old placeholder Header/Footer) — `SiteNav` already takes `auth`/`bagTotal`/`bagCount` as props for exactly that hookup, which naturally happens when U1 wires real session/cart data. Verification used a new internal, production-gated `/design-preview` route + a manual (non-CI) screenshot-parity script (`apps/market/scripts/design-parity-screenshots.mjs`).

**Internal bulk listing tool + retention-engine Phase A MERGED 05/07 (PR #63, B2, batch 37):** new `/bulk-listing` + `/bulk-listing/[batchId]` routes in `apps/market` (staff-only, `admin` role) for intaking many listings at once — batch photo intake → the existing per-draft AI-draft pipeline (client-throttled to stay under the 6/min route limit) → a review/edit grid → batch publish, which loops the existing `publishDraft()` gate one item at a time (`packages/api/src/routes/v1/seller/bulk/service.ts`) — no parallel publish path — → CSV export. New tables `bulk_batches` + `inventory_items.batch_id` (migration `0025`). Rides along: retention-engine Phase A (`docs/BRIEF-retention-engine.md` §4) — an append-only `progression_events` table fed by a small name-mapping hook inside the EXISTING `dispatchEvent()` (`lib/events.ts`), not a parallel event bus; `user.followed` deliberately left unmapped (no follow feature exists yet). Codex cross-model review (money-adjacent surface) found + fixed one real bug: the batch-publish retry query only looked at `lifecycleState="owned"`, silently stranding items that flipped to `for_sale` then failed downstream on a prior attempt — fixed to retry those too (never re-touching already-published items), with a regression test. Full detail + the `loading.tsx`/Suspense build gotcha: `docs/HANDOFF-ZERO-CONTEXT.md` §11 (05/07 entry).

**Backend admin panel MERGED 05/07 (PR #70 read-only + PR #71 refund action, B3, batch 38):** `/admin/*` in `apps/market` (staff-only, `admin` role — same mechanism as `/bulk-listing`) — orders (list + detail: items, payout hold, refunds, an event timeline read straight off the existing `marketplace_events` audit table), listings, users, payouts (read-only view; release already existed), AI-draft usage/cost aggregates, a read-only fees view (never a config-edit surface), and a page for the email worker's dead-letter queue (`getFailedEmailJobs()`, shipped with B5 but had no route until now). The one write, `POST /api/v1/admin/orders/:id/refund` (PR #71), is a thin wrapper over the existing `processRefund(isAdmin: true)` core the seller-refund and admin-cancel paths already share — no new money logic, always processor-refund (Stripe), never Bushpop-held funds (AFSL rule) — Opus-reviewed clean (double-refund race closed by a DB-level partial unique index, not just an app-layer check) before Ben merged it. No new audit table was needed and no admin-role grant was needed — `admin@bushpop.com.au` already held the role on staging. Full detail: `docs/HANDOFF-ZERO-CONTEXT.md` §11 (05/07 entry).

**U1 buyer funnel core ~80% MERGED 05/07 (PRs #69/#72/#73/#74, B10, batch 38, THE LAUNCH BAR):** shipped in 4 auto-merged stages, API contract first since the pages depend on it. **PR #69**: PDP response gains `condition`/`size`/`colour`/`brand`/`measurements`/`categorySlug`/`shippingOptions`; cart items enriched with `title`/`coverImage`/`handle` (batched, no N+1); browse/search return Meili `facetDistribution`. Codex review fixed 3 real bugs pre-merge: legacy-`NULL` `shipping_option` now normalizes to `"buyer_pays"` (matches `order-totals.ts`, was returning an empty array); malformed non-numeric `measurements` jsonb is sanitized instead of 500ing the PDP read; a doc comment overstated the cart-enrichment behavior. **PR #72**: wires U0's `SiteNav`/`SiteFooter`/`MobileBottomBar` into the live `apps/market/src/app/layout.tsx` (U0's deliberate deferral) via a new non-redirecting `getOptionalCustomer()` feeding a `<Suspense>`-wrapped async `SiteHeader`. **PR #73**: PDP fact chips + measurements panel + the approved "Buyer Protection on every order" reassurance line (render-only, no fee amount); cart renders real title/image/handle; PLP/search filter bar gained brand/size/colour selects driven by facet counts + an active-filter chip row; analytics registry swapped 3 never-wired placeholder events for the 5 named in the brief (`pdp.view`/`browse.filter_applied`/`cart.add`/`checkout.step`/`order.confirmed`). **PR #74**: home's "Latest listings" moved off a CSS grid onto `Rail`/`Pcard` (grid+overflow-x silently breaks to vertical scroll — flagged 3x in the U0 handoff). **Not done, deliberately** — checkout/confirmation restyle (launch-critical, needs its own Opus-reviewed, Ben-merge-gated PR, never rushed), the full Playwright E2E path, design-parity screenshots, CWV budget check. **New finding: zero categories are seeded** in dev/staging — blocks the W3 gender-tile default and PLP category-slug filtering until real taxonomy exists. Full detail: `docs/HANDOFF-ZERO-CONTEXT.md` §11 (05/07 entries).

**U1 polish — category seed + PLP category filter + home duo tiles MERGED 05/07 (PR #78, batch 39 session B):** closed the "zero categories seeded" finding above. The taxonomy itself (`@bushpop/config` — 10 garment types × 26 leaves) was already correct; dev seed fixtures now get real `categoryId`s, a new `pnpm --filter @bushpop/api search:reindex` script backfills MeiliSearch (search-sync is event-driven and never picks up bulk seed/SQL edits on its own), the PLP `FilterBar` gained the missing category dropdown (API already returned the facet), and the home page's deferred W3 placeholder is now a live "Shop by category" duo. **W3 descoped from a literal gender split to category** — there is no gender field anywhere in the schema and the design prototype's split was fixture-only data; flagged as an open business call (add a real gender attribute later, or keep category as the permanent answer) rather than decided here. Staging still needs the same seed + reindex run (commands in PR #78's body).

**U1 checkout + confirmation restyle — PR #79 OPEN, held for Ben (batch 39 session A):** the last U1 launch-bar item, closing the "not done, deliberately" gap in the B10 entry above. `/checkout` + `/checkout/confirmation` ported onto Lit Glass — numbered option-card sections, sticky order summary with real cart items, `ExpressCheckoutElement` (Apple/Google Pay/Link, W4's "Payment Request only" route), an "It's yours." confirmation hero. Order API gained the same title/image/handle enrichment on order items that cart already had (PR #69), plus size/condition/brand — `packages/api/src/routes/v1/store/orders/service.ts`, batched across a page (no N+1). Real Stripe test-mode keys were obtained from Ben mid-session and wired into an isolated dev-stack instance (own API + own Next server) to verify the money path end-to-end: real PaymentIntent, real test-card confirm, real signed webhook delivery, real order created and rendered — see screenshots in the PR (`$50 + $10.95 + $2.50 = $63.45`, Fee Model D). **Known environment gap, not a code defect**: this sandbox's Chromium hangs indefinitely reaching `js.stripe.com` (confirmed via curl/Node fetch to the identical URL succeeding instantly) — `checkout.spec.ts`'s card-iframe-fill step needs to run in CI/staging/a normal machine for its automated pass; the spec itself is correct and complete. Held for Opus review + Ben's manual merge — never auto-merge (money-adjacent).

**U3 seller fulfilment UI MERGED 05/07 (PR #80, batch 40 session A):** seller `/dashboard/orders` (status-filtered list — ready to ship/shipped/delivered/completed/cancelled, payout+item-count+status per row) and `/dashboard/orders/[id]` (items, payout breakdown, ship-to address, tracking) in `apps/market`, following the `/dashboard/listings` convention. New client `MarkShippedForm` (mirrors admin's `RefundButton`) PATCHes the pre-existing `PATCH /seller/orders/:id/ship` endpoint, rendering only while `status==="paid"`. Codex cross-model review (read-only sandbox) caught and fixed two real bugs pre-merge: an uncaught thrown network error in the submit handler, and a double-ship race where `router.refresh()`'s async landing left the button briefly re-enabled on stale props — closed with a local `justShipped` flag. Verified live against the dev stack (real test-seller sign-up, order reassigned via SQL, full list→detail→ship→re-render flow incl. a genuine 409 on double-ship, DB restored after). **Deliberately not built:** label-download link and a seller payouts view — neither field/endpoint is exposed by the current API (checked before building), so both are new backend work for a future session, not this UI-only slice.

**Fix: broken fixture listing images MERGED 05/07 (PR #81, batch 40 session B):** closed batch-39 session C's "all 6 seed-fixture listing images broken on staging" finding. Root cause: `packages/db/src/seed.ts` inserted `inventory_item_images` rows pointing at `items/{id}/primary.jpg` **without ever uploading anything to R2** — every seeded listing's image URL 404'd. Fix: seed now synthesizes a solid-colour JPEG per fixture via `sharp` and uploads it to R2 before inserting the row, throwing (aborting the seed) on a genuine upload failure instead of silently stranding a broken reference. Deliberately does NOT reuse `apps/web/public/demo/*.jpg` — verified live via `docker exec` on staging that the engine's `.dockerignore` excludes `apps/web/*` from the api image's build context, so that approach would have failed silently in every real deploy. Codex review caught a second real bug pre-merge: `engine-ci.yml`'s test job runs `db:seed` with no R2 env vars at all — fixed by skipping the real upload only when R2 is fully unconfigured, verified both paths (skip vs. fail-loudly) end-to-end against a scratch Postgres DB. Staging's 6 existing broken rows were remediated separately (after merge) via a one-off script run inside the live api container using its real R2 credentials — verified live: all 6 `primaryImageUrl`s on `api.bushpop.xyz` now return real JPEG bytes. Conditional refund-email leg (PR #65) was skipped — still open at session end.

**B4 — listing moderation queue v1, ships dark behind `MODERATION_QUEUE_ENABLED` (batch 40, session C):** hard gate for track F (multi-vendor onboarding). Discovered the core state machine already existed forked in from piklo-v2 — `listing_reports` (pending → reviewed → {actioned, dismissed}) driving `channel_listings.hidden_at`, plus `GET/PATCH /api/v1/admin/reports` and a public buyer-report route (`POST /api/v1/store/listings/:id/report`) — but no frontend and no internal (staff-initiated) intake existed. Built exactly those two gaps: `POST /api/v1/admin/moderation/flags` (admin flags any listing directly, no reporter-cap/active-listing guards — those are the public route's abuse controls) and `apps/market/src/app/admin/moderation/page.tsx` (status-filtered queue table, manual-flag form, action buttons mapping `REPORT_STATUS_MACHINE` transitions to start-review/approve/takedown/reinstate/reopen). Both the new flag-intake route and the queue page are gated off by default; the pre-existing `admin/reports` GET/PATCH routes stay always-on (already admin-role-gated, predate this session). No migration — reused the existing `listing_reports` + `channel_listings.hidden_at` machinery entirely. `docs/takedown-process.md` §8 updated to reflect the queue is built (not the bespoke `counterfeit_claims` table originally spec'd there — the forked schema already covers it).

**Checkout E2E now runs in real CI + staging CWV measured (batch 40, session D):** added an `e2e` job to `engine-ci.yml` (real pg/redis/meili services, migrate+seed, `tsx`-run API server, Playwright's own `webServer` boots `apps/market`) so `checkout.spec.ts` gets a genuine automated run instead of PR #79's noted sandbox gap. Real Stripe test-mode keys (from Ben, this session) + a repo-generated shared webhook-HMAC secret wired in as GitHub Actions repo secrets. Landed on `main` via PR #85 (a concurrent session's Lint-gate fix branch forked off this session's branch mid-flight in the shared main checkout and its squash-merge absorbed this session's commits too — recovered cleanly via an isolated worktree, no force-pushes over anyone's work; the separately-opened CI PR #84 was closed as superseded once confirmed byte-identical to what landed). **Real result once run against PR #79's own branch: both checkout tests failed** — not the old "Chromium can't reach js.stripe.com" gap, but a new one: card-fill + pay-button click both completed, yet the page never reached `/checkout/confirmation` (30s timeout, no captured errors). Posted in full on PR #79, not investigated further or fixed (Ben's money-adjacent merge gate). Staging CWV via Lighthouse: `/` LCP 5.0s/CLS 0.012, `/browse` LCP 3.0s/CLS 0.008 — both over the 2.5s LCP budget, CLS fine, filed as a finding only (pre-existing, unrelated to PR #79's diff).

**Seller payouts view + shipping-label download (batch 42, session B):** closed the two gaps U3 (PR #80) flagged as "not built — no field/endpoint exposed": `GET /api/v1/seller/payouts` (`packages/api/src/routes/v1/seller/payouts/`) lists a seller's own `payout_holds` rows joined to `orders` for scoping (`eq(orders.sellerId, ...)`), plus a per-status total computed via SQL `GROUP BY`/`SUM` — no arithmetic anywhere outside SQL/`formatMoney`. New `apps/market` `/dashboard/payouts` page (same `requireAuth()` + sibling `loading.tsx` convention as `/dashboard/orders`). **Real gap found and fixed en route:** the shipping-label worker (`workers/shipping-label.ts`) generated a real `labelUrl` from the provider but never persisted it anywhere — added `orders.shipping_label_url` (migration `0026`, text, nullable) and one field write in the worker; surfaced as a "Download shipping label" link on `/dashboard/orders/[id]` when present. Deliberately left `shippingLabelId` untouched — it's a money-safety "is this order tracked" signal read by `payout-hold-service.ts`, not a URL field, despite the similar name. Cross-seller scoping proven by a real integration test (two sellers, two orders, real Postgres, real Fastify HTTP) before anything else was built. **Known environment gap, not a code defect:** couldn't verify the new page/link in a live browser this session — the shared sandbox's file-watcher count was already exhausted by other concurrent sessions' dev servers (`EMFILE: too many open files, watch`), so every route including `/` 404'd under `next dev` regardless of this diff; verification instead rests on the integration-test suite (real DB, real HTTP) plus a clean `pnpm turbo typecheck`/`build` across all 7 touched packages.

## Key constraints

- **Pure RSC** — no `"use client"` on content pages. Interactivity = isolated leaf client components only.
- **Trailing-slash parity** with WordPress (`trailingSlash: true`). This is load-bearing for SEO.
- **Content site stays static** — `apps/web` keeps `output: 'export'` + CF Pages; the engine (API, DB, Stripe, auth, workers) lives in `packages/*` + `apps/market` and deploys separately (Coolify). Never import engine packages from `apps/web`.
- **No `[channel]` routing** — Bushpop is single-tenant. Flat routes in both apps. The engine keeps channel *tables* (one seeded `bushpop` row) but no channel URL segments or hostname rewrites.
- **CI split** — `deploy.yml` = content site only (install, typecheck AND build are all `--filter @bushpop/web...` since PR #30 — engine packages are never installed in the content pipeline); `engine-ci.yml` = path-filtered engine gates (build/lint/typecheck/integration tests/webpack build/cache audit/security). An engine failure must never block a content deploy. Engine container builds: `R2_PUBLIC_URL` must be passed as a Docker build arg to `apps/market` (wired in the prod compose; PR #30) or listing images break.

## Next.js 16 gotchas (from piklo-v2 AGENTS.md)

- `proxy.ts` not `middleware.ts` for channel/proxy logic (N/A at Launch 1, but carry it forward)
- `next-env.d.ts` is gitignored — Next rewrites it between build/dev modes
- `tsconfig.json` `jsx` flips between `react-jsx` (build) and `preserve` (dev) — committed as `preserve`
- No `eslint` key in `NextConfig` (removed in Next 16). **NOTE: there is currently NO wired ESLint config** (no `eslint.config.*` at repo root or in `apps/web`; the `apps/web` `lint` script `eslint src` fails). CI gates are `pnpm typecheck` + `pnpm build`, NOT lint — rely on those.
- Turbopack is default for both `next dev` and `next build`
- `cacheComponents: true` is for the SSR/Launch-2 path — NOT set here (static export)
- **MDX plugins must be passed to `createMDX` as serializable string names, NOT imported functions** — Turbopack rejects function refs with "does not have serializable options". Use `remarkPlugins: [["remark-gfm"]]` / `rehypePlugins: [["rehype-slug"]]`. Both are wired: `remark-gfm` is load-bearing for pipe tables (without it MDX tables render as literal `|` text), `rehype-slug` gives headings `id`s for in-page anchor links (e.g. `/guides/size-charts/#condition-guide`)
- **`next/image` needs `images.unoptimized: true`** under `output: 'export'` (no optimisation server). Set in `next.config.ts`.
- **No icon library ships brand marks** — Instagram/Facebook/YouTube/Twitter etc. aren't in `@phosphor-icons/react` (or lucide, which this repo no longer uses at all — fully removed in the B11 re-skin, PR #61). Inline minimal brand SVGs where needed (see `site-footer.tsx`).
- **`@phosphor-icons/react` needs the `/dist/ssr` subpath in RSC components** — `import { Heart } from "@phosphor-icons/react/dist/ssr"`, not the bare package. The base package's icons depend on a module-scope `IconContext` (`React.createContext`) that breaks under some Next 16 build configs (confirmed: crashes `apps/market`'s build under `cacheComponents`+Turbopack — see `phosphor-icons-breaks-next16-build` memory); the `/dist/ssr` entrypoint's context-free per-icon components avoided that entirely here (confirmed clean across all 57 `apps/web` routes). Pass `weight="bold"` per icon, not via a context provider.

## `apps/market` gotchas (Launch-2 engine SSR app)

- **Tailwind v4 content detection doesn't cross workspace package boundaries** — `packages/ui` (the shared `@bushpop/ui` component library) is a separate workspace from `apps/market`, so any utility class used ONLY inside a `packages/ui` component (e.g. `Button`'s `bg-brand`, `hover:bg-brand-hover`, `active:bg-interactive`) never gets scanned by `apps/market`'s Tailwind build — the class reaches the HTML but has no matching compiled CSS rule, silently rendering unstyled/invisible (white-on-white). Fixed by an explicit `@source "../../../../packages/ui/src";` in `apps/market/src/app/globals.css` (PR #55, 04/07). If a new shared package with Tailwind classes is added, it needs its own `@source` line, or its components will render broken with no build error or console warning.
- **better-auth's client requires an absolute `baseURL`, even for same-origin requests** — `createAuthClient({ baseURL: "/api/auth" })` throws `Invalid base URL: /api/auth` at runtime because better-auth validates via `new URL(...)`, which rejects relative paths. `apps/market/src/lib/auth-client.ts` uses `window.location.origin + "/api/auth"` on the client (SSR still uses `WEB_URL`) — requests still go same-origin through the `/api` proxy rewrite, only the client config needs the absolute form. Fixed in PR #53, 04/07.
- **A new route doing auth-gated RSC data fetching (e.g. `requireAuth()`/`createAuthedApiClient()`) needs a sibling `loading.tsx`, or `next build` hard-fails** under this app's `cacheComponents` config: "Uncached data was accessed outside of `<Suspense>`". `/sell` and `/dashboard/listings` were already safe because they have a `loading.tsx` — Next's file convention wraps the page in an implicit `<Suspense>` boundary when one exists. This isn't documented anywhere else; found building `/bulk-listing` (PR #63, 05/07) which had none.
- **The role/auth gate itself must live in each `page.tsx`, not a shared `layout.tsx`** — a `loading.tsx` in a segment wraps that segment's *page* in Suspense, not its *layout*; a layout that calls `requireAdmin()`/`createAuthedApiClient()` runs outside any Suspense boundary regardless of sibling `loading.tsx` files below it, and hits the same build failure as the bullet above. `apps/market/src/app/admin/layout.tsx` (B3, PR #70, 05/07) is deliberately just nav chrome with zero data fetching — every `/admin/*` page calls `requireAdmin()` itself, mirroring the existing `/bulk-listing` pages.

## Deploy

GitHub Actions (`.github/workflows/deploy.yml`) → `wrangler pages deploy out --project-name bushpop-v2`, run with `workingDirectory: apps/web`. **The `workingDirectory` is load-bearing, not cosmetic:** `wrangler pages deploy` resolves the Pages Functions directory (`functions/`) relative to its own cwd, not the output-dir argument — running it from repo root with `apps/web/out` as the arg silently never finds `apps/web/functions/`, so no Function ever deploys. This bit us for weeks (17/06–02/07): `functions/uncategorized/[[path]].js` was scaffolded and "done" but never actually served — fixed 02/07 (PR #20).

The deploy job only runs on `push` (`if: github.event_name == 'push'`) — **PRs get build/typecheck only, no preview deploy.** There is no per-PR live URL; the only way to see a change live pre-merge is to merge to `main` (staging) or run `pnpm build && wrangler pages deploy` locally against the same project.

CF Pages project: `bushpop-v2`
Staging URL: `bushpop-v2.pages.dev`

**Post-deploy verification (PR #36, 03/07):** the deploy job runs `apps/web/scripts/check-redirects.mjs` (59-row public-safe fixture) + `apps/web/scripts/post-deploy-check.mjs` (noindex polarity, security headers, sitemap/robots) against staging after every wrangler deploy; `.github/workflows/redirect-health.yml` repeats the fixture check daily. At cutover, flip `--base` in both to `https://bushpop.com.au` (marked `# TODO cutover`). Method doc: `apps/web/docs/redirect-verification.md`. The full 1,927-URL inventory, launch runbook, and monitoring plan are deliberately OUTSIDE this public repo (`~/projects/Bushpop/audit/` + `~/projects/Bushpop/docs/cutover/`); full inventory machine-verified 1,927/1,927 vs staging on 03/07. Gotcha: the 410 Pages Function only implements GET — HEAD returns 404; both scripts GET-fallback.

Production wiring (`bushpop.com.au` → Pages) is NOT this step — it's the cutover in MASTER-PLAN Phase 2. Pushing `main` deploys to **staging only**; production stays WordPress until the DNS cut (~9 Jul).

**Engine staging LIVE 03/07** — `market.bushpop.xyz` (web) + `api.bushpop.xyz` (api) via Coolify app `bushpop-engine` (uuid `w1be995ronuhl7092d4jr392`, deploy-key source, tracks `main`, base dir `/infra`, compose `/docker-compose.engine.prod.yml`). Deploys are API-triggered (`POST /api/v1/deploy?uuid=…`), NOT push-triggered. `/health/ready` all green (real Stripe test key; R2 bucket is **`bushpop-images`** + its r2.dev public URL — NOT `bushpop-media`); 6 fixtures seeded + Meili-indexed. Three gotchas fixed en route (PRs #41–#43): healthchecks must probe `127.0.0.1` not `localhost` (busybox wget resolves `::1`, meili binds IPv4-only); `validateEnv` treats `""` as unset (compose `${VAR:-}` injects empty strings); **Coolify resets any env whose compose default is non-empty to that default on EVERY deploy** — compose defaults must BE the real staging values, and Coolify-only overrides (e.g. `ADMIN_EMAIL`) will not stick. Seeding is manual (`docker exec <api> pnpm --filter @bushpop/db db:seed{,:categories}`) and does NOT index into Meili (search-sync is event-driven) — backfill by script if reseeding. `pk_test_` publishable key baked into the web bundle and Resend domain verified (both 03/07 — transactional email sends as `noreply@bushpop.com.au`). Outstanding: Stripe webhook secret is a placeholder until Phase 5; at cutover swap the two compose R2 defaults to `media.bushpop.com.au` + attach that custom domain to the bucket.

## Content authoring

Ben writes prose → Claude commits MDX → auto-deploy via CF Pages.

Content lives at: `apps/web/src/app/guides/`, `apps/web/src/app/shop/` etc.
MDX components: `apps/web/mdx-components.tsx` (wraps every page in `.prose-bushpop`).

## Design system (Launch-1)

The approved prototype (`~/projects/Bushpop/design/home/`) is ported and live on staging (#18, squash `b7f7160`).

- **Tokens** — `apps/web/src/app/globals.css` `@theme` block (ink/surface/green/red, radii, fonts) + component classes: glossy `.btn.green` Signature CTA, frosted `.nav`, `.pcard`, footer, marquee, `.prose-bushpop`. Green = accent only; red = sale only.
- **Fonts** — DM Sans + Inter via `next/font` in `layout.tsx` (licensed Roc Grotesk swap later). Swapped from the PR #18 Hanken Grotesk stand-in in the B11 re-skin (PR #61, 05/07) for parity with the prototype (Ben's call).
- **Components** — `apps/web/src/components/`: wordmark, button, chip, product-card, site-nav, site-footer (RSC) + client leaves: fav-button, fresh-drops, waitlist-form, mobile-bottom-bar, nav-scroll-border, mobile-nav-drawer (behind `SiteNav`'s `drawerNav` prop, defaulted OFF). Icons = `@phosphor-icons/react` (`weight="bold"`, via `/dist/ssr` for RSC) + a custom Southern Cross glyph in `icons.tsx` — swapped from `lucide-react` in the B11 re-skin.
- **Coming-soon framing** — no marketplace yet, so every marketplace CTA routes to the `/shop` "Launching soon" storefront via `src/lib/links.ts` (`COMING_SOON`); homepage demo products (`src/lib/demo-products.ts`) are illustrative. Shop/product/sell/checkout are Launch-2.
- **Waitlist** — first-party capture live (F1, 03/07): `waitlist-form.tsx` POSTs same-origin to `/api/waitlist` (Pages Function → n8n → homelab `bushpop.waitlist`), success only on 2xx, `segment` prop per the F10 contract (`buyer`|`seller`|`opshop`). Secret `N8N_WAITLIST_WEBHOOK` on the CF Pages project (repo is public — never commit the webhook URL). Welcome email live (F11a, 04/07): n8n branches on `inserted=true` → Resend send, ledger-compliant buyer copy, no re-email on dedup. Full architecture + export path: `docs/waitlist.md`.
- **Trust claims** — site copy is governed by `docs/trust-claims-ledger.md` (W3 gate closed 03/07, PR #28): every removed fabricated claim + its exact reinstatement condition. No invented numbers, no fictional people/quotes, no unbuilt features described as current, numbers rendered from real data only. Check the ledger BEFORE adding any stat/social-proof/trust copy.

## Git workflow

Solo dev, auto-merge low-risk. Per `~/.claude/rules/git-workflow.md`.
Auto-merge + `delete_branch_on_merge=true` are enabled on `benobie/bushpop-v2`.
