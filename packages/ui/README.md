# Bushpop UI

Shared design system built with Radix UI primitives, CVA (Class Variance Authority), and Tailwind v4.

Built in Phase 4 alongside the web frontend.

## Design Tokens
Design tokens are in `@bushpop/config/tailwind/tokens` + `@bushpop/config/tailwind/bushpop-overrides`.
