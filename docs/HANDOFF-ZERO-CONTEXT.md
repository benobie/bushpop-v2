# bushpop-v2 — ZERO-CONTEXT DEV HANDOFF

**Written:** 04/07/2026 · **Status:** LIVING DOCUMENT — append, don't rewrite history
**Audience:** any AI (or human) joining this repo with zero prior context. Read top to bottom once, then work.
**Scope:** the code — architecture, money path, tests/CI, deployment, debt, cutover machinery. No credentials or secret values live in this doc (env var NAMES only).

> **This is the DEV layer of a three-doc family** — link to the siblings, never duplicate them:
> - **Business:** `~/projects/Bushpop/docs/HANDOFF-ZERO-CONTEXT.md` — orientation, history, decision ledger, sequencing, legal. Business-level decisions (fees, claims, launch scope) canonically live in its §4.
> - **Design:** `~/projects/Bushpop/design/HANDOFF-ZERO-CONTEXT.md` — design system, homepage/prototype state, locked visual rules.
> - **Dev (this doc):** in-repo so it's versioned. When you learn something that changes a section, append a dated entry under §11 rather than silently rewriting history; small factual corrections in §§1–8 are fine if you log them in §11.

---

## 1. What this repo is

Bushpop is an Australian secondhand fashion marketplace (bushpop.com.au) being rebuilt from scratch after its WordPress v1 failed operationally — the business doc covers all of the why. This repo (`benobie/bushpop-v2`) holds both halves of the rebuild:

1. **`apps/web` — the Launch-1 content/SEO site.** Next.js 16 + MDX, static export, live on Cloudflare Pages staging (https://bushpop-v2.pages.dev). Carries the SEO assets (size charts, op-shop guides) ahead of the DNS cutover of bushpop.com.au.
2. **The marketplace engine — Launch 2.** A copy-and-own fork of the sibling `piklo-v2` engine (see §2 Fork provenance): `packages/*` + `apps/market`, single-tenant, own CI, own staging (market.bushpop.xyz + api.bushpop.xyz).

What does NOT live here: business/strategy docs, design prototypes, audits, and the cutover runbook — those live in the (non-git) workspace `~/projects/Bushpop/` (notably `docs/cutover/` and `audit/` there, **not** in this repo — see §8).

## 2. Architecture

### Monorepo map (pnpm workspaces: `apps/*`, `packages/*`)

| Path | Package | What it is |
|---|---|---|
| `apps/web` | `@bushpop/web` | Launch-1 content site. Next 16 + MDX, `output:'export'`, pure static + Pages Functions. Dev port 3000. No engine imports. |
| `apps/market` | `@bushpop/market` | Marketplace storefront. Next 16 SSR (React 19), better-auth, Stripe Elements, PostHog. Dev port 3002. |
| `packages/api` | `@bushpop/api` | The engine: Fastify v5 API **plus all background workers in-process** (no separate worker app). Entry `packages/api/src/index.ts` — validates env, builds server, `startWorkers()`, listens on 3333. |
| `packages/db` | `@bushpop/db` | Drizzle schema (15 modules under `src/schema/`), client, migrations (`drizzle/`), seeds. |
| `packages/api-client` | `@bushpop/api-client` | TS client generated from the API's OpenAPI spec (`openapi-fetch`); consumed by `apps/market`. |
| `packages/config` | `@bushpop/config` | Shared constants + Zod env schema: `env.ts`, `fees.ts`, `parcels.ts`, `listing-strength.ts`, `ai.ts`, `channel.ts`, taxonomy, prohibited terms. **Money maths starts here.** |
| `packages/types` | `@bushpop/types` | Shared Zod/TS types. |
| `packages/ui` | `@bushpop/ui` | Design system (Radix + CVA + Tailwind v4). |
| `services/support-order-context` | (not a workspace) | Thin Chatwoot dashboard app showing a buyer's WooCommerce order history — support tooling for the v1 site. Zero deps. |
| `infra/` | (not a workspace) | `docker-compose.dev.yml` (local Postgres/Redis/Meili) + `docker-compose.engine.prod.yml` (staging/prod engine stack). |

Stack: Node ≥22, pnpm 10.8.1, TypeScript ^5.7, Turborepo. Engine: Fastify ^5, Drizzle ~0.45, BullMQ ^5 (Redis/ioredis), MeiliSearch, Resend, Stripe (SDK pinned `22.0.0` via root `pnpm.overrides`, alongside `ioredis 5.10.1` — money-safety pins), better-auth, AWS S3 SDK (R2), `@google/genai` + `@anthropic-ai/sdk`, Sharp.

### Database topology

- **Postgres 16.** Dev via `infra/docker-compose.dev.yml`: Postgres on host port **5435**, Redis **6380**, MeiliSearch **7701** (chosen to clear life-dashboard on 5433 and piklo on 5434 — see the comment at `.github/workflows/engine-ci.yml:106`).
- **Test DB is `bushpop_test`** on the same 5435 instance. `packages/api/src/test/setup.ts` refuses to run unless `DATABASE_URL` contains `bushpop_test`, and TRUNCATEs user-data tables between tests. ⚠️ Fork-copied docs (`docs/engine/EDGE-CASES.md`, `ONBOARDING.md`, `ARCHITECTURE.md`) still say port 5433 / `piklo_test` — they are stale; this doc and `vitest.config.ts` are right.
- **Migrations:** `packages/db/drizzle/` — 24 files, `0000`–`0023`. `0000`–`0022` are **verbatim upstream piklo-v2**; fork-local migrations start at `0023_melted_havok.sql` (sell-flow: `ai_generations` table, `inventory_items` pricing/measurement/shipping columns, `listing_scores.breakdown`). Append from `0024`.
- **Prod compose** (`infra/docker-compose.engine.prod.yml`): pg/redis/meili internal-only; api published `3334:3333`, web `3210:3000`.

### Workers (all in-process with the API)

`packages/api/src/workers/index.ts` `startWorkers()` starts ~17 BullMQ workers (skipped when `NODE_ENV==="test"`): image-variants, image-cleanup, ai-draft (gated on `GEMINI_API_KEY`/`ANTHROPIC_API_KEY`), enrichment (gated on `ANTHROPIC_API_KEY`), checkout-expiry, shipping-label, email, event-consumer, search-sync, listing-score, notification-sweeper, refund, starshipit-poll, reconcile-indeterminate-ops, order-jobs-sweeper, backfill-aspect-ratios, and **payout-release — gated OFF by default** (§3). Inventory doc: `docs/engine/workers.md`.

### External integrations (wiring files)

| Service | Wired at |
|---|---|
| MeiliSearch | `packages/api/src/lib/meilisearch.ts`, `lib/search-index.ts` (index name is still `listings_piklo` — §7) |
| Cloudflare R2 | `packages/api/src/lib/r2.ts`, `lib/image-url.ts` — staging bucket `bushpop-images` (NOT `bushpop-media`) |
| Resend (email) | `packages/api/src/lib/email/resend.ts` (+ `mock.ts`); sends as `noreply@bushpop.com.au` |
| Stripe | `packages/api/src/lib/stripe.ts`; webhook `routes/v1/webhooks/stripe.ts`; payouts `lib/payout-hold-service.ts` |
| AI (listing drafts) | `packages/api/src/lib/ai/provider.ts` → `gemini.ts` (primary, Gemini 2.5 Flash-Lite) / `anthropic.ts` (escalation, Claude Haiku 4.5); config `packages/config/src/ai.ts` |
| Starshipit (shipping) | `packages/api/src/lib/shipping/starshipit.ts` (AusPost carrier), mock fallback in `lib/shipping/index.ts` |

### Fork provenance

The engine is a `git archive` copy of `benobie/piklo-v2` @ **`2419a38`** (02/07/2026) with fresh history — full recipe and rename table in `docs/engine/FORK.md`. `@piklo/*` → `@bushpop/*` across 172 files; single-tenant `bushpop` channel (`platformFeeBps: 175`); `apps/admin` + `apps/mobile` pruned. **Deliberately NOT renamed (money byte-parity):** Stripe metadata keys `piklo_payment_op_id` / `piklo_order_id` / `piklo_refund_id` / `piklo_checkout_session_id` — these are functional identifiers, do not "fix" them (§7). The 16 `docs/engine/*.md` files are curated upstream docs, each headed with a provenance blockquote.

## 3. The money path — as it exists today

Every step below is cited against code as at `main` 04/07/2026 (post-PR #27). This is what IS, not what's planned.

**1. Draft → publish.** Sellers build listings through the drafts API (`packages/api/src/routes/v1/seller/drafts/routes.ts`: create :52, per-step PATCHes :97-155, images :159-192, publish :196). Publish is server-gated by `publishGateMissing()` (`.../drafts/publish-service.ts:63-100`) — a **required-fields checklist** (photos, title, category, size unless exempt, condition, price>0, shipping, parcel, `legal_agree`), plus a prepaid-economics check (:85-95): for prepaid shipping, `price − fee − label` must be positive or publish 422s with `price_too_low`. ⚠️ **Correction to other docs:** the listing-strength score does **not** gate publish — it's computed *after* the gate (`publish-service.ts:197`, rubric in `packages/config/src/listing-strength.ts`) and used only for event metadata + the seller email. The business doc's glossary says otherwise; the code wins.

**2. AI listing drafts (confirm-not-commit).** `packages/config/src/ai.ts` sets Gemini 2.5 Flash-Lite primary, Claude Haiku 4.5 escalation (on throw, schema-fail, or confidence < 0.4 — `packages/api/src/workers/ai-draft.ts:38-85`). The worker writes **only `ai*` suggestion columns**, never canonical fields (`ai-draft.ts:120-137`); the seller confirms suggestions via normal PATCHes, and at publish `recordAiOutcome` diffs kept-vs-edited per field (`publish-service.ts:107-144`).

**3. Checkout → PaymentIntent.** The live storefront (`apps/market/src/components/checkout/checkout-flow.tsx:73`) posts to `/api/v1/store/checkout` → `initiateCheckout()` (`packages/api/src/routes/v1/store/checkout/service.ts`). DB-before-Stripe: reserve inventory + insert `checkout_sessions` row, then `stripe.paymentIntents.create({ amount: totals.totalCents, transfer_group, metadata }, { idempotencyKey: sessionId })` at **:287-302** — a **plain charge into the platform account**: no `transfer_data.destination`, no `application_fee_amount`. Bushpop is merchant-of-record at charge time. ⚠️ **Correction to the §10-item-2 handoff's assumption (verified live 04/07):** the PI's metadata at creation is `{buyerId, channelId, checkoutSessionId, sellerId}` — it does **not** carry `piklo_payment_op_id` or any other `piklo_*` key. Those keys are attached later, only on refund/transfer objects during reconciliation (`webhooks/stripe.ts:172-199`, `readPikloOpId`), never on the original charge/PI.

**4. Where the live trace stops (updated 04/07 — §10 item 2 CLOSED).** There is no placeholder-key short-circuit in production code — with an unset/placeholder `STRIPE_SECRET_KEY` the trace dies at the real Stripe call (`checkout/service.ts:287`; error path :310-320 → session `failed`, inventory released, 502 `STRIPE_ERROR`). Engine staging has a **real Stripe test key**, so the PI leg works there. A real `STRIPE_WEBHOOK_SECRET` is now configured (webhook endpoint `we_1TpUVBLALzoTA6yYX5BunUZG` → `https://api.bushpop.xyz/api/v1/webhooks/stripe`, all 8 events enabled), and the full chain was verified live end-to-end with Stripe test cards: a $39.70 success-card purchase produced exactly one `orders` row (`paid`), one `payout_holds` row (`held`, `amountCents` = sellerProceeds to the cent), inventory flipped `sold`/`sold`, and both buyer + seller Resend emails sent (confirmed via API container logs — the Resend API key configured on staging is send-only and can't list sends). A decline-card (`4000000000000002`) checkout correctly left the session `failed`, created zero orders, and released inventory back to `available`/`for_sale`. **The trace now stops at the shipping label** — see the new gap noted in step 8.

**5. "Direct mode".** The Phase-1 direct-mode *flag* from the roadmap does **not exist in code** — no `PAYMENTS_MODE`/`directMode` anywhere. The live `/checkout` path already *behaves* as direct/MoR (step 3). A second, **unwired** path exists (`/checkout-groups`, `routes/v1/store/checkout-groups/service.ts`) that branches by seller count (`:252` — 1 seller = Stripe destination charge, >1 = separate charges & transfers); the storefront does not call it. Building "direct mode" is therefore a formalisation task, not a flag flip. ⚠️ **Updated 04/07 (evening):** Ben ratified the §9 direct-mode row; `/checkout-groups` is now **env-gated OFF by default** — its route is only registered when `MULTI_VENDOR_CHECKOUT_ENABLED === "true"` (`server.ts`, mirrors `PAYOUT_RELEASE_ENABLED`). With the flag unset (the default everywhere), `POST /api/v1/store/checkout-groups` 404s and the path is absent from the Swagger `/docs` listing. This also closes the $0-Buyer-Protection-fee bypass that PR #58's review flagged (§10 item 4) — the endpoint is unreachable until Phase 2 wires its webhook/expiry/reconciliation legs.

**6. Webhook → order.** `routes/v1/webhooks/stripe.ts` (signature-verified raw body :76, dedup + dead-letter :84-98) handles `payment_intent.succeeded/requires_action/payment_failed`, refund events, `transfer.updated`, `account.updated` (:104-165). Order creation linearises on the `orders` INSERT with `onConflictDoNothing({ target: checkoutSessionId })` (:450-468); the insert winner marks inventory sold, writes `order_items`, **inserts a `payout_holds` row** (`amountCents = sellerProceedsCents`, status `held`/`blocked` :516-533), then enqueues order jobs (:718-737).

**7. Emails.** Resend via the email worker (`packages/api/src/workers/email.ts`, 3 attempts/backoff). Eight types in `packages/api/src/lib/email/templates.ts`: buyer order confirmation, seller order notification, buyer shipping confirmation, tracking-exception admin alert, score nudge, report actioned/reinstated, listing published. Sender name is the dynamic channel name — resolves to "Bushpop" (`packages/config/src/channel.ts`); **no "Piklo" strings in any customer-facing email**.

**8. Shipping label.** Order jobs enqueue `workers/shipping-label.ts` → provider `createShipment` → Starshipit (AusPost) when `STARSHIPIT_API_KEY` is set, else a mock (`lib/shipping/index.ts:16-29`). Label **cost** is not fetched live — deductions use static `PARCELS` estimates (`packages/config/src/parcels.ts:21-25`: small 855¢, medium 1095¢, large 1660¢; live AusPost rates explicitly out of scope day 1). ⚠️ **Gap found 04/07 (live trace, §10 item 2):** on staging, every shipping-label job fails — `Starshipit createShipment failed: 403 Forbidden`, all 3 retry attempts, then dead-letters. `STARSHIPIT_API_KEY` is set (26 chars) but `STARSHIPIT_SUBSCRIPTION_KEY` is empty; Starshipit's API gateway typically requires both. Not a code bug: `getShippingProvider()` picks Starshipit purely on `STARSHIPIT_API_KEY` presence (:16-29) with **no runtime fallback to mock** when the live call fails — a bad/missing key just fails the job outright. **Needs Ben:** check/regenerate the Starshipit subscription key and set `STARSHIPIT_SUBSCRIPTION_KEY` in Coolify.

**9. Commission + payout (verified maths).** `packages/config/src/fees.ts`: `COMMISSION_SCHEDULE = [{ effectiveFrom: "2026-07-01", bps: 175, fixedCents: 30 }]` (:22-24) — **1.75% + 30¢**, seller-side. `calcFeeCents = round(price × 175/10000) + 30` (:41-47); `calcPayoutCents = price − fee − prepaidLabel` (:54-61); order-level equivalents in `packages/api/src/lib/order-totals.ts` (`sellerProceedsCents = totalCents − platformFeeCents − prepaidLabelCents`, :74). **Worked example from the constants:** $200 sale + Medium prepaid label → fee = round(20000×175/10000)+30 = 380¢; payout = 20000 − 380 − 1095 = **18525¢ = $185.25 exactly** (asserted in the `fees.ts` docstrings and `packages/api/src/test/unit/fees.test.ts`). **Buyer total (Fee Model D, merged 04/07 via PR #58):** `subtotal + shipping + buyerProtectionFeeCents` — 4% of the posted-items subtotal + 50¢ on posted orders, $0 on pickup, no cap (`packages/config/src/fees.ts` `calcBuyerProtectionFeeCents`, `order-totals.ts`). Seller proceeds are unaffected by this fee.

**10. Payout release (gated OFF).** Holds release via `releasePayoutHold()` (`lib/payout-hold-service.ts`) → `stripe.transfers.create` to the seller's Connect Express account (idempotency key per attempt, advisory lock, list-first-after-5xx). Scheduled every 30 min by `workers/payout-release.ts`, but the worker only starts when `PAYOUT_RELEASE_ENABLED=true`, with a live-key guard requiring `PAYOUT_RELEASE_ALLOW_LIVE=true` for `sk_live_` keys (`workers/index.ts:92-104`). Manual release: `POST /api/v1/admin/payouts/:holdId/release`.

### ✅ RESOLVED 04/07/2026 — Fee Model D (was: 7% Buyer Protection fee code vs copy conflict)

Fee Model D (task `8ecbbbcf`) merged via PR #58, closing the conflict this section used to document. Current state: seller commission 1.75% + 30¢ (unchanged); buyer Buyer Protection fee 4% of item subtotal + 50¢ on posted orders, $0 on pickup, no cap, never fee-gated (`packages/config/src/fees.ts` `BUYER_PROTECTION_SCHEDULE`, `packages/api/src/lib/order-totals.ts`). `apps/web` copy and the engine now agree. See §9's Fee Model D row and §11's session E entry for full detail, test evidence, and the cross-model review findings.

**Still open from that PR:** `/checkout-groups` (a separate, UI-unreachable multi-seller endpoint) currently charges $0 Buyer Protection fee for any cart — flagged by both reviewers (Codex: high-severity bypass; Opus: revenue gap). Session C's direct-mode memo (PR #51, already merged) independently recommends gating that endpoint off by default; a follow-up code PR is filed (`~/.claude/handoffs/2026-07-04-bushpop-checkout-groups-gate.md`) and awaiting Ben's ratification — see §10 item 4.

## 4. Test & CI state

- **API suite** (`packages/api`): Vitest 4, `fileParallelism: false`, 59 test files (unit in `src/test/unit/`, 47 integration suites in `src/test/integration/`, plus route/lib-colocated). Needs **real Postgres + Redis + MeiliSearch** (`vitest.config.ts` points at `…:5435/bushpop_test`, `:6380`, `:7701` — start `infra/docker-compose.dev.yml` first). **Live-run reconciled 04/07/2026** against `main` @ `560f0ef` — `pnpm --filter @bushpop/api test` (dev stack via `infra/docker-compose.dev.yml`, migrated + seeded per the `engine-ci.yml` Integration Tests steps): **51 test files passed, 8 skipped (59 total)**; **568 tests passed, 21 skipped, 31 todo (620 total)**; 0 failures, 184.9s. The "568" figure from the PR #27 merge is now machine-verified rather than hearsay — coincidentally the same count.
- **Market tests** (73 unit/RTL + 7 real-stack Playwright E2E, vitest/RTL/MSW/Playwright tooling) landed on `main` with PR #48 (merged 04/07). Not re-run/reconciled against `main` in this doc yet — the §4 API-suite figure above is still the only live-run-verified count.
- **CI — 3 workflows** (`.github/workflows/`):
  - `deploy.yml` ("CI + Deploy") — push + PR, **no path filter**: Build job (content site only, `--filter @bushpop/web...`); Deploy job push-only → CF Pages + post-deploy checks. **PRs get no preview deploy.**
  - `engine-ci.yml` ("Engine CI") — path-filtered to engine paths: Build, Lint, Type Check, Integration Tests (service containers: pg/redis/meili, migrate + seed, full API suite), Market Build (webpack), Cache Audit, Security (`pnpm audit --audit-level=critical` + gitleaks).
  - `redirect-health.yml` — daily 22:00 UTC fixture check against staging (§8).
- ⚠️ **`main` has no branch protection** — zero required checks (`gh api .../branches/main/protection` → 404). Green CI before merge is solo-dev convention, not enforcement. Don't assume a red PR can't merge.

## 5. Deployment topology

- **Content site → Cloudflare Pages**, project `bushpop-v2`, staging https://bushpop-v2.pages.dev. Deployed by `deploy.yml` running `wrangler pages deploy out --project-name bushpop-v2` with `workingDirectory: apps/web` — **load-bearing**: wrangler resolves `functions/` relative to its own cwd; run from repo root and the Pages Functions silently vanish (this hid the 410 Function for weeks; fixed PR #20). Production bushpop.com.au stays on WordPress until the DNS cutover.
- **Engine staging — LIVE since 03/07**: `market.bushpop.xyz` (web) + `api.bushpop.xyz` (API) via Coolify app `bushpop-engine` on the homelab VPS, base dir `/infra`, compose `docker-compose.engine.prod.yml`. Deploys are **API-triggered via Coolify** (`POST /api/v1/deploy?uuid=…`) — nothing in GitHub Actions ships the engine. Gotchas (all documented in `.claude/CLAUDE.md` §Deploy): Coolify **resets any env var whose compose default is non-empty back to that default on every deploy** (hence compose defaults = real staging values, PR #43); healthchecks must probe `127.0.0.1` not `localhost` (PR #41); `validateEnv` treats `""` as unset (PR #42); seeding is manual and doesn't Meili-index.
- **Env/secrets surface (NAMES only — values live in Coolify / CF / GitHub secrets):**
  - Engine API (Zod schema, `packages/config/src/env.ts` — authoritative): required `DATABASE_URL`, `REDIS_URL`, `MEILISEARCH_HOST`, `MEILI_MASTER_KEY`, `BETTER_AUTH_SECRET`, `WEB_URL`, `ADMIN_URL`, `API_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STARSHIPIT_API_KEY`, `STARSHIPIT_WEBHOOK_SECRET`; optional `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL` (effectively required for the storefront), `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `ADMIN_EMAIL`, `STARSHIPIT_SUBSCRIPTION_KEY`, `SENTRY_DSN`; defaults `NODE_ENV`, `API_HOST`, `API_PORT`, `CHANNEL_SLUG`.
  - Market app: `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`, `API_BASE_URL` (NEXT_PUBLIC_* are build-time Docker ARGs), `PAYOUT_RELEASE_ENABLED`, `PAYOUT_RELEASE_ALLOW_LIVE`.
  - Engine runtime feature toggles (raw `process.env`, NOT in the Zod schema — same category as the payout flags above; off unless literally `"true"`): `MULTI_VENDOR_CHECKOUT_ENABLED` — gates the multi-seller `/checkout-groups` route registration in `server.ts`, **default OFF** (Phase-2 legs unbuilt; see §3.5/§9).
  - Compose extras: `BUSHPOP_DB_PASSWORD`, `POSTGRES_DB/USER/PASSWORD`, `MEILI_ENV`, `MEILI_NO_ANALYTICS`.
  - Content site / CF Pages: `N8N_WAITLIST_WEBHOOK` (Pages secret), `NEXT_PUBLIC_WAITLIST_ENDPOINT`; CI: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`.
  - `services/support-order-context`: `ORDER_CONTEXT_TOKEN`, `PORT`, `WC_BASE_URL`, `WC_CONSUMER_KEY`, `WC_CONSUMER_SECRET`.

## 6. Open PRs + gates (as at 04/07/2026, evening)

- **No open PRs.** PR #48 (`feat(market): Phase 2 sell wizard`) merged `71a5532`. Its two post-merge human gates (real-device mobile-Safari pass, staging AI live-smoke test) — **status not confirmed in this doc; ask Ben, don't assume done.**
- **PR #58 (Fee Model D) merged by Ben** `2026-07-04T15:07:27Z` (`c6e66f5`) — see §9's row and §11's session E entry for full detail. **Staging redeploy triggered immediately after (this session) since Coolify deploys are API-triggered, not push-triggered** — container start times showed the running build was still from 14:32 UTC (pre-merge) right up until this redeploy; see §11 for the outcome. **Still open from that PR:** `/checkout-groups` charges $0 Buyer Protection fee for any cart (Codex: high-severity bypass; Opus: revenue gap) — needs Ben's decision, tied to §10 item 3's ratification.

## 7. Known debt / de-hardcode register (branding)

The `@piklo/*` → `@bushpop/*` package rename is complete, and **customer-facing surfaces are clean** (emails render "Bushpop" via the channel config). The remaining "Piklo" footprint, by disposition:

| Item | Where | Disposition |
|---|---|---|
| Stripe metadata keys `piklo_payment_op_id`, `piklo_order_id`, `piklo_refund_id`, `piklo_checkout_session_id`, `piklo_reason` | `lib/refund-service.ts`, `lib/payout-hold-service.ts:633`, `workers/payout-release.ts`, `workers/reconcile-indeterminate-ops.ts`, `routes/v1/webhooks/stripe.ts` (`readPikloOpId`), `checkout/service.ts:610` | **MUST STAY** — functional reconciliation identifiers, byte-parity with Stripe objects (FORK.md §6). Renaming = a Stripe-data migration, not find/replace. |
| MeiliSearch index name `listings_piklo` | `packages/api/docker-entrypoint.sh`, search wiring | **Data migration required** — reindex under a new name + cutover, not a rename. Low urgency. |
| Stale fork-copied docs | `docs/engine/OPS-RUNBOOK.md` (entire deploy section describes upstream piklo infra — contradicts the live Coolify reality), `ARCHITECTURE.md` (piklo channel @8% examples, `DEFAULT_CHANNEL "piklo"`, `media.piklo.com.au`), `EDGE-CASES.md` (`piklo_test`/5433), `ONBOARDING.md` (5433), `workers.md` (`admin@piklo.com.au` claim — code default is `admin@bushpop.com.au`) | **CLOSED 04/07/2026** — de-Piklo docs sweep session. OPS-RUNBOOK's deploy section rewritten against the live Coolify reality (`bushpop-engine` app, API-triggered deploys, env-clobber/healthcheck/seeding gotchas); ARCHITECTURE.md channel table + `DEFAULT_CHANNEL` + R2 media host fixed; EDGE-CASES/ONBOARDING/AGENTS-upstream.md port+DB refs (5433→5435, `piklo_test`→`bushpop_test`) fixed repo-wide; workers.md admin-email default fixed. NOT touched (out of docs-only scope): the Refunds/Disputes/Label-Voiding/Stripe-Dashboard *prose* in OPS-RUNBOOK still says "Piklo runs/subscribes..." generically — stylistic, not a live-fact error, left for a future pass. |
| Provenance-header typo | Every `docs/engine/*.md` header reads "with `@bushpop/*` renamed `@bushpop/*`" (should be "`@piklo/*` renamed `@bushpop/*`") | **CLOSED 04/07/2026** — fixed across all 15 files in one pass. |
| Cosmetic comments/Dockerfile headers | `checkout/service.ts:580` ("Piklo uses Stripe Connect destination charges" — true only of the unwired checkout-groups path), `apps/market/src/lib/format-money.ts:3`, `packages/api/Dockerfile:1`, `apps/market/Dockerfile:1`, `packages/{ui,api-client}/README.md`, `infra/docker-compose.engine.prod.yml:4`, `packages/db/drizzle/0018_….sql:6` | **PARTIALLY CLOSED 04/07/2026** — fixed the docs-adjacent ones: `packages/api/Dockerfile:1` (also corrected stale "13 workers" → 17), `apps/market/Dockerfile:1`, `packages/{ui,api-client}/README.md` (ui README also fixed a dead `piklo.css` tailwind-export reference → the real `tokens`/`bushpop-overrides` exports), `infra/docker-compose.engine.prod.yml:4`. Still open: `checkout/service.ts:580`, `format-money.ts:3`, `drizzle/0018_….sql:6` — all under `packages/api/src`/`apps/market/src`, out of scope for a docs-only session (session E owns that surface this batch); safe to fix whenever next touching those files. |

There is no separate de-hardcode plan doc — **this table is the register**; tick items off via §11 entries.

## 8. Cutover machinery (content-site → bushpop.com.au)

- **The live redirect contract is in this repo:** `apps/web/public/_redirects` (69 active rules; 75×301, 1×302, 10×410-fallback, 4×200 serve-guards) + the Pages Function `apps/web/functions/uncategorized/[[path]].js` (real HTTP 410 for the WP-era spam URLs — CF Pages has no native 410 in `_redirects`). Rules are first-match; the file header documents the **CF Pages ~130–186 rule silent-drop limit** that caused the 30/06 P0 (56 category 404s). ⚠️ The 410 Function is **GET-only — HEAD returns 404**; both check scripts GET-fallback for this.
- **Trap:** `~/projects/Bushpop/audit/redirect-map.csv` + `audit/cloudflare/` are **STALE — do not regenerate or trust them**; re-running `build_redirect_map.py` would regress the 29/06 no-410/dedup decisions. This repo's `_redirects` + Pages Function are canonical.
- **Verification tooling** (`apps/web/scripts/`): `check-redirects.mjs` (verifies status/location per URL without auto-following; `--fixture` mode = `fixtures/redirect-fixture.csv`, 59 rows, runs post-deploy in `deploy.yml`; `--inventory` mode = the full 1,927-URL GSC inventory, machine-verified 1,927/1,927 on 03/07) and `post-deploy-check.mjs` (noindex/security-headers/sitemap hygiene). `redirect-health.yml` re-runs the fixture daily against staging; its `--base` flips to `https://bushpop.com.au` at T+24h post-cutover.
- **The runbook lives OUTSIDE this repo**: `~/projects/Bushpop/docs/cutover/` (launch-runbook.md, monitoring-plan.md, risks.md, GSC baselines) and the URL inventory in `~/projects/Bushpop/audit/`. (The business doc's §11 table says `bushpop-v2/docs/cutover/` — that path doesn't exist; corrected here and flagged in its amendments log.)

## 9. Decisions register (dev-level; business decisions live in the business doc §4)

| Date | Decision | Author | Evidence |
|---|---|---|---|
| 02/07/2026 | Fork = `git archive` copy of piklo-v2 @ `2419a38`, fresh history, single-tenant `bushpop` channel; keep `piklo_*` Stripe metadata keys for money byte-parity | Ben + Claude (fork session) | `docs/engine/FORK.md` |
| 02/07/2026 | Root pnpm overrides pin `stripe 22.0.0` + `ioredis 5.10.1` (money-safety) | Claude (fork session) | root `package.json` |
| 03/07/2026 | Commission cutover to 1.75% + 30¢ with prepaid-label payout deduction; effective-dated `COMMISSION_SCHEDULE` (append-only) | Ben (ratified), PR #27 | `packages/config/src/fees.ts` |
| 03/07/2026 | Publish gate = required-fields checklist incl. prepaid `price_too_low` economics; listing strength stays advisory | PR #27 cross-model review (task 9) | `publish-service.ts:83-95` |
| 03/07/2026 | AI drafts: confirm-not-commit (suggestions never write canonical fields); Gemini primary, Haiku escalation, no queue retries | PR #27 | `packages/config/src/ai.ts`, `workers/ai-draft.ts` |
| 03/07/2026 | Payout-release worker ships gated OFF (`PAYOUT_RELEASE_ENABLED`) with an sk_live_ guard | PR #27 | `workers/index.ts:92-104` |
| 03/07/2026 | Engine staging on Coolify with API-triggered deploys; compose env defaults = real staging values (Coolify clobbers stored envs on deploy) | Claude (staging session), PRs #41–#43 | `.claude/CLAUDE.md` §Deploy |
| 03/07/2026 | Post-deploy redirect verification in CI + daily `redirect-health.yml`; fixture-based smoke on every deploy | PR #36 | `apps/web/scripts/` |
| 04/07/2026 | PR #48 (sell wizard) merge held for human sign-off; post-merge gates = real-device Safari + staging AI smoke | Ben (operating model) | business doc §3/§9 |
| 04/07/2026 | This doc created as the dev layer of the three-doc zero-context family | Claude Code (Fable 5) | this file, §11 |
| 04/07/2026 | **RATIFIED by Ben 04/07/2026:** no runtime `PAYMENTS_MODE` flag — `/checkout` already IS Phase-1 direct mode by construction (absence of Connect fields, not a config value); `/checkout-groups` env-gated OFF by default via `MULTI_VENDOR_CHECKOUT_ENABLED` (mirror `PAYOUT_RELEASE_ENABLED`, `workers/index.ts:92-104`) until Phase 2 builds its missing webhook/expiry/reconciliation legs. Implemented in `server.ts` (register call wrapped) + gate test; ships OFF everywhere — PR held for Ben's merge (money-adjacent) | bushpop-q10 session C (proposed, Sonnet 5); ratified by Ben, implemented by Claude Code (Opus 4.8) | memo PR #51 ("docs: direct-mode formalisation memo"); `checkout/service.ts:287-302`; `checkout-groups/service.ts:267-268,445-451`; `server.ts` gate; `routes/v1/webhooks/stripe.ts` (no `orderGroup` matches); `workers/reconcile-indeterminate-ops.ts:77` |
| 04/07/2026 | Payment trace verified live end-to-end past the PI: webhook→order→payout-hold→emails all confirmed working with real Stripe test-card checkouts (success + decline). Shipping-label integration found broken on staging (Starshipit 403, missing `STARSHIPIT_SUBSCRIPTION_KEY`) — not a code bug, flagged for Ben to fix credentials | bushpop-q10 session D (Claude Code, Sonnet 5) | §3 steps 3/4/8; this session's §11 entry |
| 04/07/2026 | Fee Model D implemented: `packages/config/src/fees.ts` `BUYER_PROTECTION_SCHEDULE` (4% + 50¢ posted, $0 pickup, no cap); `order-totals.ts` buyer total now includes it, seller proceeds untouched; checkout Stripe charge + UI line item updated. **Independently corroborates session C's row above:** the checkout-groups BP-exclusion fix needed for this PR confirms `/checkout-groups` is a live bypass (Codex: HIGH severity; Opus: revenue-gap, not money-safety) — same endpoint session C already recommended gating OFF. **PR #58 MERGED by Ben** `2026-07-04T15:07:27Z`; staging redeploy triggered same session (see §6, §11) | Claude Code (Sonnet 5), cross-model (Codex) + Opus reviewed, merged by Ben | this file §11, PR #58 (branch `feat/fee-model-d`) |

## 10. Continue-here queue (priority order)

1. **PR #48 — ✅ MERGED 04/07** (`71a5532`), lands the market test tooling on `main`. Its two post-merge gates (real-device mobile-Safari, staging AI live-smoke) — **status unconfirmed in this doc, ask Ben, don't assume done.**
2. **Payment trace past the PI** — ✅ CLOSED 04/07: real `STRIPE_WEBHOOK_SECRET` set on staging, webhook→order→payout-hold→emails verified end-to-end (success + decline test cards). New follow-up: Starshipit shipping-label integration is broken (403, missing `STARSHIPIT_SUBSCRIPTION_KEY`) — needs Ben to fix credentials, no code change required.
3. **Formalise Phase-1 direct mode** — ✅ **RATIFIED + IMPLEMENTED 04/07 (evening).** Ben ratified the §9 row (no runtime `PAYMENTS_MODE` flag; env-gate `/checkout-groups` OFF). Implemented on branch `fix/gate-checkout-groups`: `MULTI_VENDOR_CHECKOUT_ENABLED` wraps the route registration in `server.ts` (default OFF everywhere), plus a registration-gate test + doc updates. **PR open, CI green, merge HELD for Ben** (money-adjacent per his instruction — do NOT auto-merge). This also discharges item 4's still-open BP-bypass (the gated route is unreachable). Closes on merge.
4. **BP-fee decision — ✅ DECIDED + IMPLEMENTED + MERGED 04/07** (Fee Model D, task `8ecbbbcf`, PR #58, merged by Ben `15:07:27Z`): `apps/web` copy and the engine now both match 1.75%+30¢ seller / 4%+50¢-posted-$0-pickup buyer. Staging redeploy triggered same session — see §6 for outcome. §3's old code-vs-copy warning is now closed (§3 §9 marker). ~~**Still open:** `/checkout-groups` charges zero Buyer Protection fee for any cart~~ — ✅ **RESOLVED via item 3's gate (04/07 evening):** Ben chose "gate off"; `MULTI_VENDOR_CHECKOUT_ENABLED` env-gates the endpoint OFF by default (PR held for merge on `fix/gate-checkout-groups`), so the $0-BP bypass is unreachable until Phase 2 builds BP support into that path.
5. **De-Piklo docs sweep** — §7 "safe to rewrite/fix" rows: OPS-RUNBOOK rewrite against the real Coolify deploy, 5433→5435 corrections, provenance-header typo.
6. **Test-count reconciliation** — run the API suite against a live dev stack and record the real count in §4 (replaces the "568 as recorded" phrasing).
7. **Multi-vendor (Phase 2)** per the L2 roadmap (`~/projects/Bushpop/docs/launch-roadmap-2026-07-01.md`) — order/payout work + moderation queue; `/checkout-groups` and Connect onboarding code already exist as starting points.

## 11. Session contributions (append-only)

Future Claude Code / AI sessions: **append** an entry here — author-tagged (account + model + date). Never rewrite or delete someone else's entry. If your work changes facts in §§1–8, correct them in place AND note the correction in your entry. Business-level decisions you uncover belong in the business doc's §4 — flag them here as "PROMOTE → business doc", don't decide them.

**Template:**

```markdown
### DD/MM/YYYY — <author account> · <model> · <session focus>
- What changed: …
- Facts corrected in §§1–8: … (or "none")
- PROMOTE → business doc: … (or "none")
- Queue items added/closed (§10): …
```

### 04/07/2026 — Ben's account (bobrien9@gmail.com) · Claude Code (Fable 5) · dev-layer handoff creation
- What changed: created this document (§§1–11) as the dev layer of the three-doc zero-context family; all §3 money claims verified against code (commission constants, $200→$185.25 worked example, publish gate, payout gating); filled the business doc's "Engine & repo detail" section with a 1-page summary linking here.
- Facts corrected in §§1–8 (vs what other docs claim): (a) listing strength does NOT gate publish — required-fields checklist does (`publish-service.ts:63-100`); (b) the Phase-1 "direct mode" flag does not exist in code — the live `/checkout` path is already implicitly MoR; (c) test env is Postgres 5435/`bushpop_test`, not the 5433/`piklo_test` in stale engine docs; (d) cutover runbook lives at `~/projects/Bushpop/docs/cutover/`, not `bushpop-v2/docs/cutover/`; (e) "568/568" tests is a PR #27-recorded figure, not asserted in-repo (59 test files on `main`).
- PROMOTE → business doc: (1) **the 7% Buyer Protection fee conflict** — engine implements no BP fee (`order-totals.ts:73`), shipped `apps/web` copy claims buyer-side 7% at checkout AND "no seller commission" (`page.tsx:63,68-69` + about/guides MDX), both contradicting the ratified 1.75%+30¢-seller-side / no-buyer-fees positioning — needs a §4 decision before any fee copy or fee code ships; (2) the business doc glossary's "Listing strength — …score gating publish" line is wrong per (a); (3) `main` has no branch protection — whether to add required checks before more money-path merges is an operating-model call.
- Queue items added/closed (§10): seeded the initial queue (7 items); none closed.

### 04/07/2026 — Ben's account (bobrien9@gmail.com) · Claude Fable 5 (Cowork) · pre-share final review of the three-doc family
- What changed: nothing in this doc — reviewed all three zero-context handoffs before Ben shares them with a second AI account. This doc's facts verified as the freshest of the three (created 04/07, code-cited).
- Facts corrected in §§1–8: none here. The "pending owner sweep" items from the previous entry were applied to the **business doc** this session (its §3 + §11 cutover path → `~/projects/Bushpop/docs/cutover/`, path verified on disk; §12 listing-strength glossary corrected). Design doc §1 clarified: Launch 1 is staging-live, cutover pending.
- PROMOTE → business doc: none new. Of the previous entry's three: (1) BP-fee conflict — already tracked (business §12 ⚠️ + task 8ecbbbcf); (2) glossary — fixed this session; (3) branch protection on `main` — now explicitly logged as an open Ben decision in the business doc's amendments log.
- Queue items added/closed (§10): none.

### 04/07/2026 — ben@bushpop.com.au / Claude Fable 5 (Claude Code) · §10 queue review + AFK fleet planning
- What changed: reviewed all 7 §10 items against live state (PR #48 CI all-green re-verified; prod compose `${STRIPE_WEBHOOK_SECRET}` confirmed default-less, so a Coolify-stored value survives deploys). Composed and authored a 5-session AFK fleet (`~/.claude/handoffs/2026-07-04-bushpop-q10-*.md` + launcher): A = item 5 de-Piklo docs sweep 🟢, B = item 6 test-count reconciliation 🟢, C = item 3 direct-mode decision memo 🟢 (docs-only; any checkout-groups gating is a Ben-gated follow-up), D = item 2 payment trace 🔴 (blocked-start on Ben's Stripe-dashboard + Coolify webhook-secret steps, then autonomous), E = item 4 Fee Model D implementation 🟡 (wrapper over `~/projects/Bushpop/docs/handoffs/HANDOFF-fee-model-D-implementation-2026-07-04.md`; Codex/Sonnet build, Opus deep review on the money path, cross-model mandatory, **merge HELD for Ben — never auto-merge**).
- Facts corrected in §§1–8: none. The §3 ⚠️ BP-fee code-vs-copy warning STANDS until session E's PR merges — but its status changed: **Fee Model D was DECIDED by Ben 04/07** (task `8ecbbbcf`; seller 1.75% + 30¢ unchanged; buyer BP fee 4% of item subtotal + 50¢ posted / $0 pickup, no cap, no promo; decision authority business doc §4). E's §11 entry closes the warning when the code + copy land.
- PROMOTE → business doc: none — Model D is already ledgered in business §4; item 1's merge + two human gates and the pickup handover-photo $-threshold (~$100 anchor) remain open Ben actions.
- Queue items added/closed (§10): none closed yet — items 2/3/5/6 dispatched as fleet sessions D/C/A/B; item 4 unblocked (DECIDED) and dispatched as session E; item 1 stays Ben-only; item 7 needs its own planning session.

### 04/07/2026 — ben@bushpop.com.au · Claude Code (Sonnet 5) · §10 item 6 — test-count reconciliation
- What changed: ran the full API suite live against `main` @ `560f0ef` in an isolated worktree (`infra/docker-compose.dev.yml` up, migrate + seed mirroring `engine-ci.yml`'s Integration Tests job exactly, then `pnpm --filter @bushpop/api test`). Result: **51 test files passed, 8 skipped (59 total)**; **568 tests passed, 21 skipped, 31 todo (620 total)**; 0 failures, 184.9s wall time. The PR #27-era "568 passing" figure is now machine-confirmed, not re-derived to a different number — same count, now with an authoritative source. Checked PR #48: still OPEN (not merged), so per the dispatching session's instruction the "Market tests exist only in open PR #48" line in §4 was left untouched and no market-suite run was attempted.
- Facts corrected in §§1–8: §4's API-suite sentence rewritten from "recorded at the PR #27 merge... re-derive if you need it authoritative" to the live-run figures + command + SHA (this entry).
- PROMOTE → business doc: none.
- Queue items added/closed (§10): item 6 closed (test-count reconciliation done).

### 04/07/2026 — ben@bushpop.com.au · Claude Code (Sonnet 5) · bushpop-q10 session C — direct-mode decision memo (§10 item 3)
- What changed: authored the direct-mode decision memo (full text in this PR's body). Recommends no runtime `PAYMENTS_MODE` flag, and recommends env-gating `/checkout-groups` OFF by default (follow-up code PR, filed as a handoff, merge held for Ben) rather than leaving it as-is or deleting it. Added the §9 PROPOSED row above.
- Facts corrected in §§1–8: none directly, but two findings sharpen existing §3/§7 claims: (a) `routes/v1/webhooks/stripe.ts` has **zero** `orderGroup`/`order_group` references — the entire webhook→order→payout-hold pipeline in §3.6 covers `checkout_sessions` only, and `workers/reconcile-indeterminate-ops.ts:77` explicitly comments that order_group-linked payment ops are "skip[ped] here (W3+ reconciliation path)" — `/checkout-groups` is genuinely mid-build (ADR-015 Sprint 1b W2 per its test-file header), not a finished-but-unused path; there is also no expiry sweep for `order_groups` (`workers/checkout-expiry.ts` only selects from `checkoutSessions`). (b) `/checkout` and `/checkout-groups` both call the **same** `calculateOrderTotals()` (`lib/order-totals.ts`) — there is no fee-model divergence between them today, and none can emerge once Fee Model D lands there, since it's one shared function. This corrects the session E handoff's working assumption that checkout-groups "predates Model D" and needs divergence-flagging — it structurally can't diverge.
- PROMOTE → business doc: none — flag/no-flag and route-gating are dev-level implementation decisions within this queue item's scope, not business calls.
- Queue items added/closed (§10): item 3 delivered as PROPOSED (not closed — awaiting Ben's ratification of the §9 row). Filed a follow-up code handoff for the `/checkout-groups` env-gate: `~/.claude/handoffs/2026-07-04-bushpop-checkout-groups-gate.md` (cross-model review required, merge held for Ben — this session touched docs only, per its [MONEY-PATH] boundary).

### 04/07/2026 — ben@bushpop.com.au / Claude Code (Sonnet 5) · session A, de-Piklo docs sweep (§10 item 5)
- What changed: closed the §7 "safe to rewrite/fix" rows. `docs/engine/OPS-RUNBOOK.md`'s "Production Deploy" section rewritten wholesale as "Engine Staging Deploy" against the live Coolify reality (app `bushpop-engine`, uuid, base dir `/infra`, API-triggered deploys not push-triggered, market/api.bushpop.xyz domains, the env-var Coolify-clobber/127.0.0.1-healthcheck/empty-string-unset gotchas as a three-row table, manual seeding without Meili backfill, real Stripe test key already wired vs placeholder webhook secret); also fixed its stale Local Development/Backup sections (`docker-compose.yml`→`docker-compose.dev.yml`, `piklo-db`→`bushpop-db` container/user names). `ARCHITECTURE.md`: single-channel `bushpop` table replacing the two-channel piklo/bushpop table (with a note that `channels.platform_fee_bps` is legacy — real commission comes from `fees.ts`'s `COMMISSION_SCHEDULE`), `DEFAULT_CHANNEL` "piklo"→"bushpop" (2 places), R2 media host fixed to the r2.dev reality. `EDGE-CASES.md`, `ONBOARDING.md`, `AGENTS-upstream.md`: all `piklo_test`/5433 → `bushpop_test`/5435 (also caught `AGENTS-upstream.md`, not explicitly named in the handoff but hit by the same grep). `workers.md`: `admin@piklo.com.au` → code-accurate `admin@bushpop.com.au`. Provenance-header typo ("with `@bushpop/*` renamed `@bushpop/*`") fixed across all 15 `docs/engine/*.md` files in one pass. Optional row: fixed `packages/api/Dockerfile:1`, `apps/market/Dockerfile:1`, `packages/{ui,api-client}/README.md` (ui README also corrected a dead `piklo.css` tailwind-export reference to the real `tokens`/`bushpop-overrides` exports, verified against `packages/config/package.json` + `apps/market/src/app/globals.css`), `infra/docker-compose.engine.prod.yml:4` — comment-only, zero build-behaviour change.
- Facts corrected in §§1–8: none in this doc (all corrections landed in `docs/engine/*.md`, this repo's §7 table updated in place to tick the closed rows — see above).
- PROMOTE → business doc: none.
- Queue items added/closed (§10): item 5 closed. Left deliberately untouched (out of docs-only scope, flagged for a later pass): the Refunds/Disputes/Label-Voiding/Stripe-Dashboard *prose* in `OPS-RUNBOOK.md` still generically says "Piklo runs/subscribes..." (money-path behavioural docs, not deploy/infra — same category as the §7 "Cosmetic comments" row's still-open code-comment items in `checkout/service.ts:580`, `format-money.ts:3`, `drizzle/0018_….sql:6`, all under `packages/api/src`/`apps/market/src` and owned by session E this batch).

### 04/07/2026 — ben@bushpop.com.au / Claude Code (Sonnet 5) · bushpop-q10 session D — payment trace past the PI (§10 item 2)

- What changed: Ben created the test-mode Stripe webhook endpoint (`we_1TpUVBLALzoTA6yYX5BunUZG` → `https://api.bushpop.xyz/api/v1/webhooks/stripe`) and set `STRIPE_WEBHOOK_SECRET` in Coolify (two rows exist — production + preview; both updated, only the production row is actually consumed since this app has no preview deployments); redeployed via the Coolify API, `/health/ready` all green post-deploy. Found and fixed via the Stripe API (config-only, not code): the endpoint was subscribed to `account.external_account.updated` instead of `account.updated`, and was missing `charge.refunded` entirely — corrected to the exact 8-event list the handler switches on (`webhooks/stripe.ts:106-160`). Ran the full autonomous trace against `market`/`api.bushpop.xyz` driven entirely via curl (signed up a fresh test buyer, created an address, added a seeded fixture to cart, initiated checkout, then confirmed the resulting Stripe PaymentIntent directly via the Stripe API using test payment methods — `pm_card_visa` for the success leg, `pm_card_chargeDeclined` for the decline leg — since there is no browser tool available in this session). **Success leg** ($30 item + $9.70 shipping = $39.70 total): PI succeeded, webhook delivered (`pending_webhooks: 0`), `checkout_sessions.status` → `succeeded`, exactly one `orders` row (`paid`, $39.70), exactly one `order_items` row, inventory flipped `sold`/`sold`, `payout_holds` row `held` at $38.87 (= total − platform fee, matches §3.9 pre-Model-D maths), both buyer + seller Resend emails sent (confirmed via API container logs — the Resend API key on staging is send-only, can't list sends via API), and `PAYOUT_RELEASE_ENABLED` confirmed unset on the api container. **Decline leg** ($45 item, `4000000000000002`): PI declined (`card_declined`/`generic_decline`), `checkout_sessions.status` → `failed`, zero orders created, inventory released back to `available`/`for_sale`. **New gap found:** the shipping-label job fails 403 against Starshipit on every attempt (all 3 retries) — `STARSHIPIT_SUBSCRIPTION_KEY` is empty on staging; not a code bug, `getShippingProvider()` has no fallback-to-mock on a live-call failure.
- Facts corrected in §§1–8: (a) §3.3 — PI metadata at creation does NOT carry `piklo_*` keys (only `buyerId`/`channelId`/`checkoutSessionId`/`sellerId`); those keys only appear later on refund/transfer objects during reconciliation. (b) §3.4 rewritten — the verified live trace now extends through webhook→order→payout-hold→emails (was previously "stops at the webhook"); the new end-of-trace is the shipping-label gap. (c) §3.8 — documented the Starshipit 403 gap and the shipping provider's lack of a live-failure fallback.
- PROMOTE → business doc: none — the Starshipit credentials gap is an ops/infra fix, not a business-level decision.
- Queue items added/closed (§10): item 2 closed. New follow-up (not queued as a formal item, just flagged): Ben needs to check/regenerate the Starshipit subscription key and set `STARSHIPIT_SUBSCRIPTION_KEY` in Coolify before shipping labels will work on staging.

### 04/07/2026 — ben@bushpop.com.au · Claude Code (Sonnet 5) · session E, Fee Model D implementation (task `8ecbbbcf`)
- What changed: implemented Fee Model D end to end. `packages/config/src/fees.ts` adds `BUYER_PROTECTION_SCHEDULE`/`calcBuyerProtectionFeeCents` (effective-dated, mirrors `COMMISSION_SCHEDULE`). `packages/api/src/lib/order-totals.ts` now computes `buyerProtectionFeeCents` from the posted-items subtotal only (pickup items excluded); buyer `totalCents` includes it, `sellerProceedsCents` derives from a pre-fee total and is untouched. Wired through `checkout/service.ts` (Stripe PaymentIntent `amount` now charges the correct total), `checkout/schemas.ts`, `orders/schemas.ts` + `orders/service.ts`, the Stripe webhook's order-insert, and a new additive migration `0024_loud_agent_zero.sql` (`buyer_protection_fee_cents integer DEFAULT 0 NOT NULL` on both `checkout_sessions` and `orders`). Frontend: `checkout-flow.tsx` and `order-summary.tsx` render a "Buyer Protection" line item (never "insurance"), shown only when > 0. Fixed the misleading `:580` comment in `checkout/service.ts` (falsely implied this direct-mode path uses Stripe Connect destination charges — only `checkout-groups` conditionally does). Full copy sweep across `apps/web` (homepage, about/how-it-works, about/selling, complete selling guide, vinted-australia guide, how-to-buy guide, shop meta) killing "7%", "no seller commission", "costs nothing" claims; `docs/trust-claims-ledger.md` §12 added recording every change. Also lightly annotated two out-of-repo build-week planning docs (`build-week-cowork-orchestration.md`, `build-week-trust-claims-copy.md`) as superseded — `design/home/fees.html`, `design/brand/voice-guide.md`, `design/home/content-ledger.md` were already correctly swept by the Cowork session that authored the implementation spec, verified clean, not re-edited.
- **checkout-groups divergence (per this wrapper's explicit instruction not to rebuild it):** that path's `computeSellerTotals` was patched to force `totalCents = subtotalCents + shippingCents` (excluding BP) rather than use the shared function's BP-inclusive value — this was necessary to stop the Stripe `application_fee_amount` destination-charge withholding from over-withholding the BP amount from a seller's Connect transfer (a real money bug the fix prevents). Net effect, confirmed by BOTH cross-model reviewers: `/api/v1/store/checkout-groups` now charges **zero** Buyer Protection fee for any cart, including single-seller ones, and it is a live, mounted, authenticated endpoint (`server.ts`) not called by any current UI (`checkout-flow.tsx` only posts to `/api/v1/store/checkout`). **Codex rated this a HIGH-severity bypass** (a crafted authenticated request to `/checkout-groups` avoids the fee entirely); **Opus rated the same fact a LOW-severity "revenue gap, not a money-safety bug"** (no one is overcharged/undercharged, it's an unwired path that simply doesn't implement the new fee yet). Both agree the local divergence fix itself is correct and sufficient for its narrow goal (no BP leaks into seller withholding). **This needs an explicit Ben decision before/at launch: gate `/checkout-groups` until Phase 2 adds BP support, or accept the risk given it's UI-unreachable today.** Not resolved in this PR per the explicit out-of-scope instruction — flagged in the PR body.
- Also flagged (Codex, moderate confidence, self-resolving): a `payment_pending` checkout session created just before this migration/deploy lands will have `buyerProtectionFeeCents=0` baked in (migration default) and, since the reuse path returns stored totals verbatim, would complete at the old (BP-free) total if the buyer returns to an already-open session post-deploy. Self-resolves within `CHECKOUT_EXPIRY_MINUTES=30`; noted as a deploy-ordering consideration, not a code fix.
- Facts corrected in §§1–8: §3's "no buyer protection fee at all" and "no BP constant/line-item exists" claims are now stale — superseded by this session's implementation, but per this handoff's own instructions the §3 warning text itself is left for whoever is at the keyboard after Ben merges to remove (business-doc §13 note + Ben task `8ecbbbcf` closure are also post-merge). §9 gets a new row (this session) rather than editing the commission row, since seller commission itself is unchanged.
- PROMOTE → business doc: the checkout-groups bypass decision above (gate vs accept) needs Ben's call; currently only recorded here + the PR body, not yet in the business doc.
- Queue items added/closed (§10): none closed (PR not merged yet — closure is post-merge per this session's own wrapper). Adds: once merged, formalising `/checkout-groups` (queue item 3, "Formalise Phase-1 direct mode") should also resolve the BP-bypass decision, not just the direct-mode flag question.

### 04/07/2026 (evening) — ben@bushpop.com.au · Claude Code (Sonnet 5) · fleet-results audit + zero-context handover refresh

- What changed: audited the full 04/07 §10 fleet's actual outcome against live git/GitHub/Coolify state (not assumed from the launcher's plan) and brought this doc back to accurate. **While auditing, PR #58 (Fee Model D, session E) was merged by Ben** (`2026-07-04T15:07:27Z`, `c6e66f5`) — confirmed via `gh pr view 58` (`mergedBy: benobie`), not an unauthorised auto-merge. Checked whether staging actually reflected the merge: the content site (`apps/web` → Cloudflare Pages) auto-deployed via `deploy.yml` on push, already live with the new copy by `15:07:29Z` (verified: `bushpop-v2.pages.dev` homepage now reads "Buyer Protection fee on posted orders — free for local pickup"). The **engine** (`apps/market` + `packages/api` → Coolify `bushpop-engine`) does NOT auto-deploy on push (§5) — its running containers were still from the 14:32 UTC pre-merge build. Triggered a Coolify redeploy this session (API call, self-service per established practice); new containers came up `2026-07-05T01:12:34+10:00`/`15:12:34Z`, `/health/ready` green. Both halves of the site are now running the merged Fee Model D code.
- Doc corrections applied (facts stale from having been written mid-fleet, before later PRs landed): §4 market-tests line (was "exists only in open PR #48" — PR #48 merged, tests are on `main`); §6 "Open PRs" section rewritten from scratch (was describing PR #48 as open/held; now: zero open PRs, PR #58 merge + redeploy recorded, the still-open checkout-groups bypass called out); §9's Fee Model D row updated from "PR opened, held for Ben — not merged" to merged + redeployed; §10 item 1 updated from "Merge PR #48 after sign-off" (stale — already merged) to merged + gate-status-unknown; §10 item 4 updated from "DECIDED... PR open, held for Ben" to DECIDED + IMPLEMENTED + MERGED, with the checkout-groups follow-up preserved as still-open.
- Facts NOT changed (checked, still accurate): §9 direct-mode PROPOSED row (session C) — still awaiting Ben's ratification, unaffected by the Model D merge except that Model D's own review now independently corroborates the checkout-groups bypass concern, raising the stakes on ratifying it. §10 item 7 (multi-vendor) — untouched, still needs its own planning session.
- PROMOTE → business doc: none new — the checkout-groups gate decision was already flagged by session E's entry above; not duplicating.
- Queue items added/closed (§10): none newly closed by this session (items 1–6 were already at their current state or closed by prior sessions this batch; this entry only corrects stale phrasing). **What a new session should do first**, in priority order:
  1. **Ask Ben**, don't assume: (a) has he run PR #48's two post-merge human gates (real-device mobile-Safari, staging AI live-smoke)? (b) does he want to ratify the §9 direct-mode PROPOSED row now (this also unblocks the checkout-groups-gate follow-up handoff, filed but not started: `~/.claude/handoffs/2026-07-04-bushpop-checkout-groups-gate.md`)? (c) has he decided gate-vs-accept on the checkout-groups BP-fee bypass (Codex: HIGH severity, Opus: revenue gap)?
  2. **Low-priority, not launch-blocking:** Starshipit shipping labels are broken on staging (403, missing `STARSHIPIT_SUBSCRIPTION_KEY`) — needs Ben to check/regenerate the key in Coolify, no code involved.
  3. **Not yet started:** §10 item 7, multi-vendor Phase 2 — needs its own planning session against `~/projects/Bushpop/docs/launch-roadmap-2026-07-01.md` whenever Ben wants it.
  4. As of this entry, **there is no in-flight code work and no open PRs** — the queue is genuinely caught up modulo the three Ben-decisions above.

### 04/07/2026 (evening) — ben@bushpop.com.au · Claude Code (Opus 4.8, 1M) · checkout-groups gate (§10 item 3, Ben-ratified)

- Ben's live decisions this session: (1) PR #48's two post-merge gates — **"done but sign-up failed"** (flagged: sign-up failing on staging blocks the AI live-smoke and is a live bug worth a separate look; session D signed up a test user via curl fine on 04/07, so it smells like a browser/SSR-path regression, not the API). (2) **Ratified** the §9 direct-mode PROPOSED row. (3) **Gate** `/checkout-groups` (not accept, not delete).
- What changed: implemented the ratified gate on branch `fix/gate-checkout-groups`. `packages/api/src/server.ts` — wrapped the single `await app.register(checkoutGroupRoutes)` in `if (process.env.MULTI_VENDOR_CHECKOUT_ENABLED === "true")` (raw `process.env` check mirroring `PAYOUT_RELEASE_ENABLED` at `workers/index.ts`, NOT routed through the Zod schema) + an explanatory comment so a future reader doesn't un-gate it. Added `packages/api/src/test/integration/store/checkout-groups-gate.test.ts` — builds fresh server instances per env value (the shared `getTestApp()` singleton caches the app process-wide, so it can't test both states); asserts `POST /checkout-groups` → 404 when the flag is unset/`"false"`, → not-404 when exactly `"true"`. No change to `/checkout`, checkout-groups' internal service/routes/schemas, or the existing `service.test.ts`. Compose + `.env.example` deliberately left with no flag entry (unset ⇒ OFF, matching the payout-toggle precedent; also dodges the Coolify env-clobber gotcha).
- Facts corrected in §§1–8: §3.5 updated (route now env-gated OFF; 404 + absent from Swagger when unset). §5 env surface adds the `MULTI_VENDOR_CHECKOUT_ENABLED` name as a runtime feature toggle (raw process.env, not Zod).
- PROMOTE → business doc: none — the gate-vs-accept call is now made (gate) and is a dev-level implementation decision; nothing new for the business ledger.
- Queue items added/closed (§10): item 3 RATIFIED + IMPLEMENTED (closes on merge); item 4's still-open `/checkout-groups` $0-BP bypass RESOLVED by the same gate (unreachable by default). **PR open, CI green, merge HELD for Ben** — money-adjacent, do NOT auto-merge. Cross-model review (Codex + Sonnet) summarised in the PR body.

### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · B2 — internal bulk listing tool + retention-engine Phase A (batch 37, session C)

- What changed: built the internal bulk-listing tool end-to-end on branch `feat/b2-bulk-listing`. **Backend** (`packages/api/src/routes/v1/seller/bulk/`): `POST /batches` (start an intake batch), `GET /batches` (resume list), `GET /batches/:id` (batch + all items via the existing `getDraft` serializer — zero duplicated strength/image logic), `POST /batches/:id/drafts` (create N empty drafts tagged to the batch — `createDraft()` gained an optional `batchId` param, backward compatible), `POST /batches/:id/publish` (sequential loop over `publishDraft()` — the SAME publish gate `/sell` uses, no parallel path — partial success: published/failed arrays, not all-or-nothing), `GET /batches/:id/export.csv` (crosslisting CSV: item/listing/handle/status/title/brand/category/size/colour/condition/price/description/primary-image-url). Photo intake and AI-draft enqueue deliberately reuse the EXISTING per-draft HTTP endpoints from the client (no new bulk AI/photo routes) — the 6/min ai-draft rate limit and 20/day cap are real, unchanged safety nets; the frontend throttles to concurrency 2 to stay under them. New tables: `bulk_batches` (id/ownerId/label — thin grouping tag) and `inventory_items.batch_id` (nullable FK, `ON DELETE SET NULL`), migration `0025_organic_silverclaw.sql`.
- **Retention-engine Phase A** (`docs/BRIEF-retention-engine.md` §4), riding with this build per the brief: new append-only `progression_events` table + `packages/api/src/lib/progression-events.ts`, hooked directly into the EXISTING `dispatchEvent()` (`lib/events.ts`) via a small event-name mapping table — **not** a parallel event bus. Mapped: `channel_listing.published`→`listing.published`, `channel_listing.archived`→`listing.removed`, `channel_listing.status_changed` (metadata.to==="sold")→`listing.sold`, `order.created`→`order.completed`, and a NEW `item.saved` dispatch added to `wishlist/service.ts`'s `addToWishlist()` (that path previously fired no event at all). **`user.followed` has NO mapping** — no "follow a user" feature exists anywhere in the engine (confirmed by search); fabricating a call site ahead of the feature was out of scope per the brief's own roadmap (social layer excluded v1). Also added 5 read-only SQL views for brief §7's metrics (activation, weekly-listers/habit, W4 retention, catalogue quality via strengthScore in event metadata, AI kept-rate) — hand-appended SQL in the migration (not Drizzle-schema-tracked, so future `db:generate` diffs are unaffected). No Phase B consumer — by design, per the brief.
- **Frontend** (`apps/market`): `/bulk-listing` (batch list + new-batch) and `/bulk-listing/[batchId]` (photo intake, review/edit grid, AI-draft trigger, publish, CSV export), gated by a new `lib/require-admin.ts` checking the `roles` array `/api/v1/customer/me` already returns (nobody in `apps/market` was reading it before). **The Bushpop staff account needs an `admin` row in `user_roles` granted manually (same mechanism as granting `seller`) before this route is reachable** — no admin frontend exists anywhere in this monorepo to do it from yet; this is a one-time ops SQL step, not a code gap. Built with existing `@bushpop/ui` primitives + the same `brand-*` Tailwind utilities `dashboard/listings` already uses — no new tokens, no `packages/ui`/`globals.css`/`packages/config/tailwind` touches (B9/U0's files, per this batch's carve-out). Reused `compressImageForUpload` from the sell wizard's photos-step verbatim rather than reimplementing compression.
- **Gotcha hit + fixed:** the two new pages 500'd at `next build` under Cache Components ("Uncached data was accessed outside of `<Suspense>`") because `/sell` and `/dashboard/listings` get their implicit Suspense boundary from a sibling `loading.tsx` — a fact not written down anywhere. Added `loading.tsx` for both new route segments; `next build` now completes clean (17/17 pages). Any FUTURE new route under `apps/market` doing auth-gated RSC data fetching needs a sibling `loading.tsx` or it will hard-fail the production build, not just warn.
- Facts corrected in §§1–8: none directly — this is additive surface, not a correction to existing money-path claims.
- PROMOTE → business doc: none — this is a dev-level implementation of a decision (Phase A rides with the listing tool) already ledgered there.
- Queue items added/closed (§10): none of the existing queue; this was dispatched as its own batch-37 kickoff item (B2), now delivered.
- Test note: `packages/api` full-suite runs during this session showed 21–39 failures with a DIFFERENT failing set each run, including a pure unit test (`listing-strength.test.ts`) failing on the shared `beforeEach` TRUNCATE itself — this is the shared-dev-stack contention from concurrent batch-37 worktree sessions (see `~/.claude/projects/-Users-ben-projects-bushpop-v2/memory/shared-dev-stack-test-flakiness.md`, filed by session B5 this same batch), not a regression from this branch. The new `bulk-listing.test.ts` file (7 tests) passed 100% in two separate isolated single-file runs; `pnpm --filter @bushpop/db typecheck`, `@bushpop/api typecheck`, `@bushpop/api-client typecheck`, and `@bushpop/market typecheck` are all clean, and `apps/market`'s production build succeeds.

### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · B9 — U0 design-system foundation (batch 37, session A)
- What changed: ported `design/home/bushpop.css` (Bushpop/design repo, non-git) into the engine as the new BRG "Lit Glass" token layer. **Additive, not a replacement**: `packages/config/tailwind/tokens.css` keeps the existing Coral `--color-brand-*`/`--color-trust-*`/`--color-interactive` scale byte-identical (54+ live call sites across browse/checkout/bag/dashboard/sell-wizard depend on it, none of which are this session's scope) and adds a parallel `--color-bp-*`/`--radius-bp-*`/`--shadow-bp-*`/`--font-bp-*` namespace with the BRG values copied exactly from the prototype (`--green-ink #004225` LOCKED, `--green-bright #0b5c35` LOCKED, CTA gradient stack, impact palette, obsidian, deal-red, pill/rect/card radii). `@bushpop/ui` gained: `Button` restyled onto the new F2 glass-pill primary + 12px glass-rect secondary system (existing `primary`/`outline`/`ghost`/`destructive` variant names preserved so all 14+11+9 existing call sites keep working unchanged — only their look changed); new `Chip`/`Tgl`/`Tlink`/`FoilBadge`/`Pcard`/`Rail`+`RailItem`/`SiteNav`/`SiteFooter`/`MobileBottomBar`; shared motion utilities (`useCursorLight` pointer-lerp hook, `spawnBurst` for the click-sparkle/mini-heart particles, `useNavScrolled`); all recipes live in one new stylesheet `packages/ui/src/styles/lit-glass.css` (class-prefixed `bp-`, imported once via `apps/market/src/app/globals.css`).
- **Gotcha (new, worth flagging loudly): `@phosphor-icons/react` breaks the Next 16 build under `cacheComponents`/Turbopack.** The kickoff spec called for Phosphor Bold icons; installing `@phosphor-icons/react` and importing even one icon through `@bushpop/ui`'s barrel `index.ts` made **every route** fail `next build`'s "Collecting page data" phase with `TypeError: (0, e.createContext) is not a function` (Phosphor's `IconContext` calls `React.createContext` at module scope; that evaluation appears to run under a React-server-conditioned module resolution during page-data collection that lacks `createContext`). Neither `transpilePackages` nor `experimental.optimizePackageImports` fixed it. **Fix:** dropped the dependency entirely and hand-authored the icon set as local SVG components (`packages/ui/src/icons/nav-icons.tsx` + `custom.tsx`) using the prototype's own exact path data extracted from `_icons-sprite.html`/`_nav.js`'s `SYM` object — pixel-identical to the source (better fidelity than an approximate Phosphor match) with zero new runtime dependency. If a future session wants Phosphor specifically, re-test against a plain `next build` (no `cacheComponents`) first to isolate whether the flag or Turbopack is the actual trigger.
- **Live-layout integration deferred, flagged not silently skipped:** did NOT wire `SiteNav`/`SiteFooter`/`MobileBottomBar` into `apps/market/src/app/layout.tsx` (still renders the old placeholder `Header`/`Footer`/`MobileNav`, Coral-toned, unchanged). Recommendation: do this as part of U1, where real session/cart data plumbing is being built anyway (`SiteNav` already takes `auth`/`bagTotal`/`bagCount` as props, not fixtures, specifically so U1 can wire it straight to real data). Screenshot-parity verification instead used a new internal, unlinked, prod-gated preview route (`apps/market/src/app/design-preview/page.tsx`, 404s when `NODE_ENV==="production"`) exercising every new primitive — screenshotted at desktop+390px against the live prototype (`design/home/bushpop-home-v3.html`, `button-lab.html`) via a new manual-only tool `apps/market/scripts/design-parity-screenshots.mjs` (NOT wired into `pnpm test:e2e`/CI — the comparison target is the non-git `~/projects/Bushpop` workspace, absent from any CI checkout; running it needs both a local prototype static server on :8899 and `pnpm dev` up). Visual pass: nav (both auth states), buttons (all variants), chips/toggle/text-link, FoilBadge (all 5 variants), Pcard hover/fav, footer all matched the locked recipes on inspection.
- Facts corrected in §§1–8: none.
- PROMOTE → business doc: none new.
- Queue items added/closed (§10): none — U0 was dispatched as its own batch-37 kickoff item (B9), now delivered. **Unblocks U1 (B10)**, which was gated on this merge.
- Test note: `pnpm --filter @bushpop/ui typecheck`, `@bushpop/market typecheck`, `@bushpop/config typecheck`, `@bushpop/api typecheck`, `@bushpop/api-client typecheck` all clean; `pnpm --filter @bushpop/market build` green (16/16 routes); market unit suite 73/73 passed; E2E smoke spec passed. Did **not** re-run the full `@bushpop/api` suite (568) or `sell-wizard.spec.ts` live against the shared dev stack — session C's entry immediately above independently hit the same shared-dev-stack contention this same batch (21-39 flaky failures unrelated to their diff); this session's change surface is `packages/ui` + `packages/config/tailwind` + `apps/market`'s `globals.css`/`next.config.ts`/new `design-preview` route only, and the sell wizard confirmed (grep) to import zero `@bushpop/ui` symbols, so the risk/value tradeoff of contending for the shared Postgres favoured skipping it.

### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · B5 — transactional email matrix (batch 37, session B)

- What changed: audited then closed every gap in runbook §4.1's email matrix. New living doc `bushpop-v2/docs/email-matrix.md` (public-safe mirror). **Row 3 (shipping confirmation) — CLOSED:** `workers/shipping-label.ts`'s automated Starshipit/mock label path persisted tracking info but never transitioned `orders.status` to `shipped` nor dispatched `order.shipped` — the buyer's manual mark-shipped path (`seller/orders/service.ts`) was already correct, but the automated path (the one that fires once `STARSHIPIT_SUBSCRIPTION_KEY` is set) silently skipped both the status transition and the email. Fixed to mirror the manual path exactly (CAS + `dispatchEvent`), so both producers drive the same `event-consumer.ts` handler. **Row 7 (account emails) — CLOSED, worse than flagged:** `lib/auth.ts` had **no `sendVerificationEmail`/`sendResetPassword` callback configured at all** — `request-password-reset` 400'd outright (`RESET_PASSWORD_DISABLED`), `send-verification-email` silently no-op'd. Wired both via `getEmailSender()` + two new templates; real-staging smoke run (signed up `bobrien9+bushpoptest@gmail.com` on `api.bushpop.xyz`, both endpoints returned 200, API logs confirm Resend — not mock — fired) — inbox-screenshot half of the receipt still needs a session with interactive Gmail OAuth to close out. **DLQ visibility — CLOSED:** nothing queried the email queue's failed-job list; added `getFailedEmailJobs()` + a real-BullMQ (not mocked-queue) integration test proving a permanent failure is queryable. **Row 4 (refund confirmation) — BUILT, PR #65 HELD for Ben:** did not exist in the engine's 8 templates. Wired into all 6 places `refund-service.ts` can land an order in a refunded/cancelled terminal state (both `processRefund()` success paths, both `resumePendingRefunds()` crash-recovery paths, both out-of-order webhook-reconciliation paths) — the two reconcile functions now return whether they actually finalised the order so the email fires exactly once, on whichever leg completes it, not on the leg that defers. Admin cancellations (`terminalOrderStatus: "cancelled"`) explicitly exempted from the email worker's "skip if cancelled" guard, since the buyer still needs to hear their money is coming back. Cross-model (Codex) review requested on PR #65.
- Gap surfaced along the way: the local dev stack (`infra/docker-compose.dev.yml`) is shared across every concurrent worktree session on this machine — no per-worktree isolation. Confirmed via `git stash` that unrelated full-suite failures reproduced on unmodified `main`, and confirmed non-deterministic via repeated identical single-file runs. Filed as `~/.claude/projects/-Users-ben-projects-bushpop-v2/memory/shared-dev-stack-test-flakiness.md` — sessions B2 and B9 (above) independently hit the same thing this batch.
- Facts corrected in §§1–8: §3 step 8 (shipping label) — the "buyer would never receive a shipping confirmation" gap on the automated label path is now closed, not just flagged.
- PROMOTE → business doc: none — email-matrix rules were already settled by the runbook; nothing here changes business-level scope or copy.
- Queue items added/closed (§10): none of the existing numbered queue; dispatched as its own batch-37 kickoff item (B5), now delivered (PR #62 merged; PR #65 open, held for Ben).
- Test note: PR #62 (non-money: matrix doc, shipping fix, auth emails, DLQ helper) — typecheck/lint/build clean, every new/changed test file passing reliably in isolation (verified multiple times each); merged + staging redeployed via Coolify. PR #65 (refund email, money-adjacent) — typecheck/lint/build clean, 50/50 tests passing across `refund-service.test.ts` + `email.test.ts` run together and individually; held for Ben, not yet live on staging.

### 05/07/2026 — Ben's account (bobrien9@gmail.com) · Claude Code (Sonnet 5) · B11 apps/web re-skin (styling PR-2/PR-3)
- What changed: ported the locked BRG/Lit-Glass system from `~/projects/Bushpop/design/home/bushpop.css` into `apps/web` on branch `feat/b11-web-reskin` (worktree `.worktrees/b11-web-reskin`) — PR #61. `@theme` tokens copied by value from the styling handoff §2 table (green-ink/green/green-bright, obsidian, F2 CTA gradient); `text-green-ink` renamed to `text-green-bright` site-wide (~10 files) since every use was flat link/accent text, which is the flat-primary case per Ben's ② decision. Re-skinned Button (F2 glass pill + 12px secondary rects — also fixed a shape-rule bug where ghost/dark buttons were defaulting to pill), Chip, ProductCard (treatment D + foil badges + "N% OFF" copy), FavButton, SiteFooter (obsidian), SiteNav (borderless-until-scroll via a new `NavScrollBorder` client leaf, optional `MobileNavDrawer` behind a `drawerNav` prop defaulted OFF). Swapped `lucide-react` → `@phosphor-icons/react` (removed lucide-react entirely) across all 5 consuming files + a custom Southern Cross glyph. Screenshots (desktop 1440px + mobile 390px, via headless Playwright against the static export — the Chrome extension's `resize_window` tool failed with a bounds error at every size tried this session) committed to `apps/web/docs/qa/b11-reskin/`.
- Facts corrected in §§1–8: none — apps/web is out of this doc's §3 money-path scope (static content site, no engine imports).
- PROMOTE → business doc: none — pure re-skin, no fee/copy/business-decision content. Three open asks live in the PR body instead (DM Sans vs Hanken, mobile-drawer default, icon-swap flag), since they're PR-scoped calls for Ben, not business-ledger items.
- Queue items added/closed (§10): none — this doc's queue doesn't track B11; see `~/.claude/handoffs/2026-07-04-batch-37-launcher.sh` / kickoff-prompts §B11 for that thread. ~~**PR #61 HELD for Ben — visual gate, do not auto-merge.**~~ **MERGED 05/07** — Ben reviewed live and said "merge" directly (DM Sans already confirmed same session; drawer-default and icon-swap asks accepted as shipped — drawer stays OFF, Phosphor icons stand). Squash `d4f6777`. Had to rebase onto 5 sibling batch-37 commits first (`main` had moved via PR #62/#64/#66/#67/#63) — the PR had gone `mergeable:CONFLICTING` in the interim, which silently stops GitHub from running any checks (not a CI failure, just no checks at all); `git rebase origin/main` (one trivial append-only §11 conflict, kept both blocks) + force-push restored CI, both workflows green, then merged clean.

### 05/07/2026 — Ben's account (bobrien9@gmail.com) · Claude Code (Sonnet 5) · B3 backend admin panel (batch 38)

- What changed: internal Ben-only admin panel v1, split into two PRs per the money-adjacent-write rule. **PR #70 (read-only surfaces) MERGED 05/07** (squash `8606cbc`): `GET /api/v1/admin/orders` (+ `:id` detail — items, payout hold, refunds, event timeline sourced from the existing `marketplace_events` audit table, no new audit table needed), `GET /api/v1/admin/listings` (+ `:id`), `GET /api/v1/admin/payouts` (release action already existed), `GET /api/v1/admin/ai-usage` (+ `/summary` cost/token/status aggregates over `ai_generations`), `GET /api/v1/admin/fees` (read-only view of `packages/config/src/fees.ts` — never a config-edit surface), `GET /api/v1/admin/email-jobs/failed` (thin wrapper over the already-exported `getFailedEmailJobs()` from PR #62 — it had no route until now). All gated by the pre-existing `admin` `user_roles` role (same `requireRole("admin")` mechanism `/bulk-listing` uses) — `admin@bushpop.com.au` already holds the role on staging, confirmed via `psql`, no grant needed (B2's open item 1 is already satisfied). `apps/market` `/admin/*` pages for all of the above.
- **PR #71 (refund action) OPEN, HELD for Ben** (money-adjacent, Opus-reviewed, never auto-merge): `POST /api/v1/admin/orders/:id/refund` — the runbook T-0 step-3 dependency (refunding Ben's own live smoke purchase). Delegates entirely to the existing `processRefund(orderId, actorId, reason, { isAdmin: true })` — same Stripe-refund/transfer-reversal/payment_operations-WAL machinery the seller-refund and admin-cancel paths already use, never a Bushpop-held-funds refund (AFSL rule). Writes an `order.refunded` `marketplace_events` audit row via the existing `dispatchEvent()`, mirroring the admin-cancel route's pattern exactly. Deliberately does NOT enqueue a refund-confirmation email — that email type doesn't exist on `main` yet (PR #65, refund email, still open/held for Ben) — a code comment marks exactly where to wire it once #65 lands, so the two PRs don't race each other.
- Facts corrected in §§1–8: added `POST /api/v1/admin/orders/:id/refund` to the money-path narrative — §3 step 10 (payout release) and the refund-service description (§3 doesn't currently enumerate admin routes individually; no correction needed there, this is additive).
- PROMOTE → business doc: none — pure engine build, no business-ledger decision surfaced.
- Queue items added/closed (§10): none tracked here — this is a batch-38 kickoff thread (`~/.claude/handoffs/2026-07-05-batch-38-launcher.sh` / kickoff-prompts §B3). New API test files: `packages/api/src/test/integration/admin/{read-routes,orders-refund}.test.ts` (33 tests, all passing; full suite 611 passed/21 skipped/31 todo, up from the 568-baseline + these 33 net-new — some pre-existing suite growth from sibling batch-38 PRs #68/#69 landed in between). Both PRs' `pnpm --filter @bushpop/market build` green (all `/admin/*` routes compile as PPR pages under `cacheComponents`) — the role gate lives in each `page.tsx` (`requireAdmin()`), not the shared `layout.tsx`, to avoid the known uncached-fetch-outside-Suspense build failure (see the apps/market gotchas in this repo's root `.claude/CLAUDE.md`).
- What changed: B10/U1 buyer funnel core (batch 38, session A) — shipped in 4 stages per the time-budget priority (API contract → shell wiring → PLP/PDP/cart → home v3), all auto-merged (no money/fee logic touched in any of them). **PR #69** (`packages/api`): PDP response gains `condition/size/colour/brand/measurements/categorySlug/shippingOptions`; cart items enriched with `title/coverImage/handle` (batched queries, no N+1); browse/search return Meili `facetDistribution`. Codex cross-model review caught 3 real bugs, all fixed: legacy `NULL shipping_option` now normalizes to `"buyer_pays"` (matches `order-totals.ts`, not an empty array); malformed non-numeric `measurements` jsonb entries are sanitized instead of 500ing the PDP read; the cart-enrichment doc comment was corrected to match actual (non-status-gated) behavior. **PR #72**: `apps/market/src/app/layout.tsx` now wires U0's `@bushpop/ui` `SiteNav`/`SiteFooter`/`MobileBottomBar` — U0's deliberate deferral — with a new non-redirecting `getOptionalCustomer()` (sibling to `requireAuth()`) feeding real session+cart data through a `<Suspense>`-wrapped async `SiteHeader`; exported the nav icon set from `@bushpop/ui`'s index (previously site-nav-internal only). **PR #73**: PDP fact chips + measurements panel + the approved "Buyer Protection on every order" reassurance line (render-only, no fee amount — BP fee depends on the shipping choice made at checkout); cart page renders real title/image/handle instead of "Item N"; PLP/search filter bar gained brand/size/colour selects driven by facet counts + an active-filter chip row; analytics registry swapped 3 never-wired placeholder events for the 5 named in the brief (`pdp.view`, `browse.filter_applied`, `cart.add`, `checkout.step`, `order.confirmed` — last two await the checkout PR). **PR #74**: home page's "Latest listings" swapped from a CSS grid to `Rail`/`Pcard` (grid+overflow-x silently breaks to vertical scroll — a bug flagged 3x in the U0 handoff); no gender/category duo tiles shipped (W3 default is "keep", but this session found **zero categories seeded** in the dev/staging DB — a duo tile linking to an empty category would itself be a trust-claims-ledger violation, so it's deferred until real category data exists, not fabricated).
- **NOT shipped this session (documented, not silently dropped) — checkout PR restyle, full Playwright E2E (browse→PDP→bag→checkout→confirmation), design-parity screenshots, CWV budget check.** Checkout/confirmation restyle needs its own Opus-reviewed, human-merge-gated PR per the launch-critical rule ("never ship checkout half-reviewed") — ran out of session budget before starting it cleanly; the existing `/checkout` + `/checkout/confirmation` routes are untouched and still functionally correct (BP line renders, OrderPoller works), just not yet on the Lit Glass system. Next session should start there.
- Facts corrected in §§1–8: none — pure additive API/UI work, no prior documented fact was wrong.
- PROMOTE → business doc: **zero categories are seeded in the dev/staging DB** — flagging since it silently blocks W3's gender-tile default and any category-filter UX beyond free-text brand/size/colour. Someone needs to seed real category taxonomy before U1's PLP category filter or the home duo tiles can go live with real data.
- Queue items added/closed (§10): none tracked here — batch-38 kickoff thread (`~/.claude/handoffs/2026-07-05-batch-38-launcher.sh` / kickoff-prompts §B10). New/extended API tests: `store-listings.test.ts` (+3: PDP fields, legacy-shipping-option default, malformed-measurements sanitization), `store-search.test.ts` (+1: facetDistribution), `cart.test.ts` (+1: enrichment) — `store-listings.test.ts` + `store-search.test.ts` both 14/14 green; `cart.test.ts` hit the known shared-dev-stack signup rate limit from concurrent batch-38 session B traffic (unrelated to this diff — the enrichment assertions execute before the 429s start). Market unit suite 73/73 green throughout all 4 PRs. `pnpm turbo build --filter=@bushpop/market...` green on every PR and again on the fully-merged main tip (verified against session B's admin-panel PRs #70/#71 landing in between with no conflicts).
- Gotcha for future sessions: **`git stash -u` silently drops the `-u` flag under this repo's rtk Bash hook** — untracked files survive a "successful" stash, producing a false-negative baseline comparison. Use `rtk proxy git stash push -u` to bypass. Cost me two wasted baseline-build attempts before I caught it.

### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · batch 39 session B — U1 category taxonomy seed + PLP filter + home duo tiles
- What changed: closed the "zero categories seeded" gap flagged by batch-38 session A. Verified the existing `@bushpop/config` taxonomy (10 garment types × 26 leaves) is real and sufficient — no schema/taxonomy changes needed. `packages/db/src/seed.ts`'s 6 dev fixtures now get a real `categoryId` (looked up by slug), previously all null. Added `packages/api/src/scripts/reindex-search.ts` (`pnpm --filter @bushpop/api search:reindex`) — search-sync is event-driven, so bulk seed/SQL edits never reach MeiliSearch on their own. Added a `categorySlug` Select to `FilterBar` (browse/search) — the API already returned the facet (PR #69) but no UI control existed. Replaced the home page's W3 placeholder comment with a live "Shop by category" duo (top-2 categories by listing count, real cover image + count, computed at request time). Verified live end-to-end against the dev stack (seeded, reindexed, drove a real browser — screenshots taken, category dropdown + filtering + duo-tile navigation all confirmed working). PR #78, merged.
- Facts corrected in §§1–8: none.
- PROMOTE → business doc: W3 ("gender duo section") descoped from a literal "Shop women/Shop men" split to "Shop by category" — there is no gender field anywhere in the schema (inventory_items has no such column, AI drafts don't infer it) and the design prototype's split (`design/HANDOFF-home.md`) was fixture-only (`g:'w'|'m'|'u'` hardcoded). Adding a real gender attribute is a schema + AI-prompt + sell-wizard change; flagging for a business call on whether that's worth building later, or whether "Shop by category" is the permanent answer.
- Queue items added/closed (§10): none — this was dispatched via a batch-39 handoff, not a §10 queue item. Left for a follow-up session/session C: staging (`api.bushpop.xyz`) needs the same `db:seed:categories` + `search:reindex` run (commands in PR #78's body) — staging's existing demo listings likely have no `categoryId` set either, so the duo tiles/category filter will show 0 results there until either those listings are re-published with a category or a one-off backfill UPDATE is run.

### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · U3 seller fulfilment UI (batch 40, session A)
- What changed: shipped the seller-facing fulfilment/ship UI that the U3 handoff pulled forward into the launch window. `PATCH /api/v1/seller/orders/:id/ship` already existed with no frontend — added `/dashboard/orders` (status-filtered list: ready-to-ship/shipped/delivered/completed/cancelled, with payout/item-count/status per row) and `/dashboard/orders/[id]` (items, payout breakdown, ship-to address, tracking) to `apps/market`, following the `/dashboard/listings` convention (`requireAuth()` + sibling `loading.tsx`, per the repo's cacheComponents-needs-Suspense gotcha). New client `MarkShippedForm` (mirrors admin's `RefundButton` pattern) collects carrier + tracking number and PATCHes the ship endpoint, rendering only while `status === "paid"`. Cross-linked listings ↔ orders dashboard pages. PR #80, merged.
- **Deliberately not built:** label download link and a seller payouts view. Checked the API first — `orderResponseSchema`/`formatOrder()` don't expose `shippingLabelId` or a label URL (the shipping-label worker never persists the provider's `labelUrl` anywhere, only `trackingNumber`/`trackingCarrier`), and there's no seller-facing payouts endpoint at all (only the admin `payoutHold` read-only view from B3). Both would be new backend work outside a UI-only slice — flagging as the natural next scope if Ben wants either.
- Codex cross-model review (read-only sandbox, findings only) caught two real bugs, both fixed before merge: (1) `MarkShippedForm.submit()` only handled parsed API error responses, not a thrown transport/network error — added a `catch` with an inline message. (2) A race: after a successful ship, `router.refresh()` is async but `submitting` cleared immediately, leaving a window where the button re-rendered enabled with the stale `status==="paid"` prop, letting a seller fire a second PATCH that 409s against the order they just shipped — fixed with a local `justShipped` flag that hides the button immediately, independent of when the RSC re-render lands. Also widened the status-filter query validation to the full backend enum (previously a direct link like `?status=refunded` silently fell back to "all" since only the 6 tab statuses were recognised).
- Facts corrected in §§1–8: none — additive UI only, no engine/money-path change.
- PROMOTE → business doc: none.
- Queue items added/closed (§10): none tracked here — dispatched as its own batch-40 kickoff item, now delivered.
- Test note: `pnpm turbo build --filter=@bushpop/market...` and `pnpm turbo typecheck --filter=@bushpop/market...` both green (before and after the Codex-review fixes). Verified live against the dev stack — signed up a real test seller via `/api/auth/sign-up/email`, reassigned a dev order to it via SQL, drove the full list → detail → mark-shipped → re-render flow end to end including a genuine 409 on a deliberate double-ship attempt, then restored the dev DB (order seller/status/tracking, test user + role/profile rows) to its prior state before finishing. Hit the same shared-dev-stack contention noted by prior batches: another concurrent worktree session (`u1-buyer-funnel`) already had a `next dev` server bound to the shared `apps/market` port 3002 — ran an isolated instance on a different port against my own worktree's files instead of touching that session's process.

### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · fix: broken fixture listing images (batch 40, session B)
- What changed: fixed the "all 6 seed-fixture listing images broken on staging" finding from batch-39 session C (`inventory_item_images.backfill_status = 'failed_unreadable'` on every seeded row). Root cause: `packages/db/src/seed.ts` inserted an `inventory_item_images` row pointing at `items/{id}/primary.jpg` **without ever uploading anything to R2 at that key** — the storefront's `getPublicImageUrl()` served a 404 for every seeded listing. `seed.ts` now synthesizes a real solid-colour JPEG per fixture via `sharp` and uploads it to R2 before inserting the row; an upload failure throws and aborts the seed (fails loudly) instead of silently stranding a broken image reference. First attempt reused `apps/web/public/demo/*.jpg` — caught via `docker exec` on the live staging container that the engine's `.dockerignore` excludes `apps/web/*` from the api image's build context (Launch-1 content site deploys separately to CF Pages), so that approach would have failed silently in every real deploy. Codex cross-model review caught a second real bug pre-merge: `engine-ci.yml`'s test job runs `db:seed` with no R2 env vars at all (the API test suite never fetches real image bytes) — the initial fix threw unconditionally on missing `R2_BUCKET_NAME`, which would have broken that CI job; fixed by skipping the real upload only when R2 is fully unconfigured (all three vars absent), while a genuine upload failure in dev/staging/prod (where R2 is always configured) still throws. Both paths verified locally end-to-end against a real scratch Postgres database (R2 unset → seed completes cleanly; R2 set to an unreachable endpoint → "Seed failed" + non-zero exit). PR #81, merged (Lint failed on this PR but is already failing identically on `main` — pre-existing unrelated `react-hooks/exhaustive-deps` config issue, not caused by this change; Type Check/Build/Integration Tests all green).
- Staging remediation (separate from the code fix, run after PR #81 merged): uploaded a real image + reset `backfill_status` for the 6 existing broken rows directly on the live `bushpop-engine` staging stack, via a one-off script executed inside the running api container (`docker exec ... tsx remediate-fixture-images.mjs`, using the container's already-configured real R2 credentials). Verified live: `curl` against the R2 public URL for each of the 6 images returns 200 with real JPEG bytes, and `GET /api/v1/store/listings` on `api.bushpop.xyz` returns working `primaryImageUrl` values for all 6 fixtures. Conditional email-wiring leg (refund-confirmation enqueue in the admin refund route) was in scope only if PR #65 had merged — it was still OPEN at both the start and end of this session, so that leg was skipped entirely; no email-template files were touched.
- Facts corrected in §§1–8: none — this is a dev/staging fixture-data bug, not an engine-architecture or money-path change.
- PROMOTE → business doc: none.
- Queue items added/closed (§10): none tracked here — dispatched as its own batch-40 kickoff item (session B), now delivered.
- Test note: `pnpm turbo typecheck --filter=@bushpop/db --filter=@bushpop/api` green throughout. New deps on `@bushpop/db`: `@aws-sdk/client-s3` (matches the version already pinned in `packages/api`) and `sharp` (same version as `packages/api`'s existing dependency — no new native-binary surface, already built into the api Docker image).
### 05/07/2026 — bobrien9@gmail.com · Claude Code (Sonnet 5) · B4 — listing moderation queue v1, behind a flag (batch 40, session C)
- What changed: discovered the queue's core state machine already existed — `listing_reports` (pending → reviewed → {actioned, dismissed}, driving `channel_listings.hidden_at`) plus the admin `GET/PATCH /api/v1/admin/reports` routes were forked in from piklo-v2 (`9ac1708`) with a public buyer-report intake (`POST /api/v1/store/listings/:id/report`) already wired, but had **zero frontend** and no internal (staff-initiated) intake path. Built exactly those two missing pieces rather than a parallel system: (1) `POST /api/v1/admin/moderation/flags` (`packages/api/src/routes/v1/admin/moderation.ts`) — an admin creates a `listing_reports` row directly (no reporter-cap/active-listing guards, those are the public route's abuse controls), dispatches `listing.flagged`, gated behind `MODERATION_QUEUE_ENABLED` (unset by default — new admin-flag attack surface, even though role-gated, mirrors the `MULTI_VENDOR_CHECKOUT_ENABLED` precedent). (2) `apps/market/src/app/admin/moderation/page.tsx` — the queue UI: status-filtered table (listing title/price/reporter email, enriched onto `admin/reports`' response), a manual-flag form, and `ModerationActions` buttons mapping `REPORT_STATUS_MACHINE` transitions to plain-English actions (start review / approve / takedown+confirm / reinstate / reopen). Page itself redirects to `/admin` when the flag is off — the existing GET/PATCH routes stay always-on (already admin-role-gated, predate this session, no new surface). No migration — reused `listing_reports` + `channel_listings.hidden_at` entirely, per the "reuse existing lifecycle machinery" scope note.
- Facts corrected in §§1–8: none — additive only.
- PROMOTE → business doc: **track F (multi-vendor onboarding) gate note** — the moderation queue is now built and functional (flag → review → takedown/dismiss → audit event, tested end-to-end against the real dev DB), but ships dark (`MODERATION_QUEUE_ENABLED` unset in every environment). Turning it on is a deploy-config flip, not a code change, whenever track F is ready to open.
- Queue items added/closed (§10): none tracked here — batch-40 kickoff thread, `~/projects/Bushpop/docs/handoffs/kickoff-prompts-2026-07-04.md` §B4. New API tests: `packages/api/src/test/integration/admin/{moderation,moderation-gate}.test.ts` (9 tests: flag creation, role/404/409 guards, full flag→review→takedown flow, registration-gate on/off) — full API suite 624 passed/21 skipped/31 todo (net +9 from this session's baseline). `apps/market` unit suite 75/75 green; `pnpm turbo build --filter=@bushpop/market...` green (`/admin/moderation` compiles as a PPR page identically to its sibling `/admin/*` routes).
